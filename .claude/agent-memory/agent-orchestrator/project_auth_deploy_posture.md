---
name: project-auth-deploy-posture
description: Postura auth/despliegue — 4E.1C assets oficiales ICONIC en exports DESPLEGADO (2026-06-06). Vercel supabase+db, remoto 20/20 (sin migración); main=8121731. Sin dorado; logos full/symbol embebidos
metadata:
  type: project
---

**ESTADO ACTUAL (2026-06-06) — 4E.1C activación de assets oficiales GRUPO ICONIC DESPLEGADO:** `main = origin/main = 8121731` (merge `8121731`, rama `integration/wave-4e1c-official-iconic-assets` `0f67f0a`). Production READY en `construction-ops-psi.vercel.app`. **Sin migración**, **sin cambios estructurales/financieros** ⇒ remoto **20/20** intacto. La usuaria entregó assets oficiales: `apps/web/public/branding/iconic/grupo-iconic-logo-full.png` (846×846 RGBA) y `grupo-iconic-logo-symbol.png` (1080×1080 RGBA) + guía `docs/branding/ICONIC_EXPORTS_VISUAL_GUIDE.pdf` (interna, NO publicada; smoke `/branding/...pdf` = 404). **Embed reproducible**: `scripts/branding/embed-iconic-assets.mjs` → genera `apps/web/server/estimates/export/logo-asset.ts` (`ICONIC_LOGO_FULL_DATA_URI`/`ICONIC_LOGO_SYMBOL_DATA_URI`, base64, **sin fs runtime**). **Paleta oficial única** `ICONIC_EXPORT_PALETTE` en `branding.ts` (azul #005DD6, cian #00B8FF acento, navy #020148, grafito #1B1F3E, gris azulado #C7DCED, gris claro #F2F4F7, blanco) — **DORADO #C8A24B ELIMINADO** (no reintroducir sin OK). PDF: encabezado con logo completo sobre blanco (texto navy legible), regla cian, títulos azul, footer con símbolo + paginación, TOTAL GENERAL navy+cian. Excel RESUMEN: logo completo + navy + cian; encabezados azul, totales navy+cian; estructura/fórmulas/hojas/conteos/paneles intactos. `.gitattributes` añadido (binarios como binarios). **712 tests**, build fixture+db limpio (sin warnings), gm 22/22, validador 214/0/0. Deuda `ICONIC_LOGO_ASSET` RESUELTA. **Única acción manual pendiente**: descargar Excel/PDF y validar logo/símbolo/paleta/sin dorado/total general. **4E.2 NO iniciada.** (Histórico 4E.1B/4E.1/4D.2 y anteriores abajo.)

**ESTADO PREVIO (2026-06-06) — 4E.1B branding visual de exports DESPLEGADO:** `main = origin/main = e526a61` (merge `e526a61`, rama `integration/wave-4e1b-export-branding` `5ab6c0a`). Production READY en `construction-ops-psi.vercel.app`. **Sin migración**, **sin cambios estructurales ni financieros** (solo lenguaje visual de Excel/PDF) ⇒ remoto **20/20** intacto. Identidad ICONIC en fuente única `apps/web/server/estimates/export/branding.ts` (paleta HEX/ARGB: azul noche #0F2A43, dorado #C8A24B + `loadBrandLogo`). **Logo serverless-safe vía base64 EMBEBIDO** `apps/web/server/estimates/export/logo-asset.ts` (`ICONIC_LOGO_BASE64`, hoy vacío ⇒ monograma `IC`); **NO se usa `fs` ni `outputFileTracingIncludes`** (Turbopack NFT lo avisaba; el bundled mantiene el build limpio). Ruta del PNG oficial documentada en `apps/web/public/branding/README.md`. PDF: banda de marca fija (logo/monograma), línea dorada, ficha de proyecto, secciones, filas alternas, TOTAL GENERAL resaltado, footer paginado. Excel: banda en RESUMEN, encabezados/títulos de marca, subtotales/totales resaltados, paneles congelados. **4E.1 CERRADA con smoke real** (Excel/PDF abren, 3 hojas, 14 cap/132 ítems, directo 336.084.480, total 372.247.170). **710 tests** (+7), build fixture + db limpio, gm 22/22, validador 214/0/0. **Deuda**: `ICONIC_LOGO_ASSET` (pegar el PNG oficial en base64). **Única acción manual pendiente**: descargar de nuevo Excel y PDF y validar branding/colores/logo. **4E.2 NO iniciada.** (Histórico 4E.1/4D.2/4D.1 y anteriores abajo.)

**ESTADO PREVIO (2026-06-05) — 4E.1 exportación protegida Excel + PDF DESPLEGADA:** `main = origin/main = 5f66b50` (merge `5f66b50`, rama `integration/wave-4e1-budget-exports` `0ed2745`). Production READY en `construction-ops-psi.vercel.app`. **Sin migración** ⇒ remoto **20/20** intacto. **Export del presupuesto real** (no del Excel original) por **camino nuevo sobre `EstimatesWriteRepository`**: método `getEstimateExportPayload` (db+fixture) + servicio `apps/web/server/estimates/export/` (`generateEstimateExcelExport`/`generateEstimatePdfExport`, ExcelJS + @react-pdf, deps ya aprobadas; **sin libs nuevas**). El módulo legacy `modules/exports` (Oleada 3, read-model demo) queda intacto. Ruta `GET /api/estimates/export?format=xlsx|pdf&estimateId&projectId&scopeId` (`runtime=nodejs`, `force-dynamic`): viewer requerido (sin sesión 401), cadena proyecto/alcance/presupuesto validada (cross-org 404), 400 params, 413 tamaño, sin service-role, en memoria, filename sanitizado (`ENTRE_PATIOS_PRIMER_PISO_…_V01.xlsx`). Excel: hojas RESUMEN/PRESUPUESTO/TRAZABILIDAD (trazabilidad solo hoja secundaria). PDF: sobrio/paginado, sin UUID/source_row/secretos. Reutiliza el resumen financiero 4D.2 (no recalcula). UI: sección "Exportar presupuesto" (Excel/PDF, loading, anti doble-click, error sanitizado, descarga directa). **703 tests** (+16), build fixture + db-local PASS, gm 22/22, validador 214/0/0, RLS sin cambios. Smoke prod: `/login` 200, export sin sesión **401**, export sin params **400**, detalle 307→/login. **Deudas**: `EXPORT_TRACEABILITY_BY_ROLE`, `EXPORT_PROFILES_FOR_ESTIMATE` (+ avalar el método repo con agent-db-rls). **Única acción manual pendiente**: en PRESUPUESTO BASE → V01 descargar Excel y PDF y validar visualmente V01/AIU/total general. **4E.2 NO iniciada.** (Histórico 4D.2/4D.1/4C.x y anteriores abajo.)

**ESTADO PREVIO (2026-06-05) — 4D.2 AIU editable + total general DESPLEGADO:** `main = origin/main = e5eede1` (merge `e5eede1`, rama `integration/wave-4d2-editable-aiu` `c3a0c48`). Production READY en `construction-ops-psi.vercel.app`. **Sin migración** ⇒ remoto **20/20** intacto (reutiliza `indirect_cost_rules`). AIU **por versión, editable, NO global**: repo `getEstimateVersionAiu`/`updateEstimateVersionAiu` (upsert atómico A/I/U/IVA; fixture solo lectura)/`calculateEstimateFinancialSummary`; cálculo `aiu-calc.ts` (Decimal: humano 3.5→0.035; A/I/U sobre directTotal, IVA sobre utilidad, grandTotal). UI: sección "AIU y costos indirectos" con preview client-side + cálculo definitivo server-side + total general; navegador solo envía 4 %; versión emitida read-only. **RLS runtime 93/93**, 687 tests. **4D.1 ya CERRADA** (revisión operativa con datos reales). **Única acción manual pendiente**: en PRESUPUESTO BASE → V01 escribir Administración 3.5 / Imprevistos 2.5 / Utilidad 4 / IVA 19, Guardar y verificar el total general. **Deudas**: AIU_PRESETS_BY_ORGANIZATION, AIU_IMPORT_PREFILL_FROM_EXCEL. **4D.3/4E NO iniciadas.** (Histórico 4D.1/4C.x y anteriores abajo.)

**ESTADO ACTUAL (2026-06-05) — 4D.1 revisión operativa del presupuesto DESPLEGADA:** `main = origin/main = e3ad4af` (merge `e3ad4af`, rama `integration/wave-4d1-budget-review` `dbb3c50`). Production READY en `construction-ops-psi.vercel.app`. **Sin migración** ⇒ remoto **20/20** intacto. Métodos de lectura RLS-bound en `EstimatesWriteRepository` (db+fixture): `listChaptersByEstimateVersion`/`getChapterById`/`listItemsByChapter` (+ `getEstimateById` para el resumen). UI: detalle del presupuesto con resumen (V01/capítulos/ítems/total directo) + tabla de Capítulos (indicador "normalizado" si source_code≠code) + ruta `…/chapters/[chapterId]` con tabla de Ítems BOQ + trazabilidad secundaria. **4C.3 ya CERRADA con smoke real**: la usuaria importó el Excel histórico ORIGINAL → V01 contiene datos reales (14 capítulos/132 ítems). 666 tests, gm 22/22, validador 214/0/0. **Única acción manual pendiente**: abrir PRESUPUESTO BASE, revisar resumen y entrar a 2-3 capítulos para validar los ítems importados. **4D.2/4E NO iniciadas.** (Histórico 4C.3/4C.x y anteriores abajo.)

**ESTADO ACTUAL (2026-06-05) — 4C.3 normalización reversible de códigos DESPLEGADA:** `main = origin/main = 4071be8` (merge `4071be8`, rama `integration/wave-4c3-source-normalization` `2594042`). Production READY en `construction-ops-psi.vercel.app`. Migración `20260605120000_boq_source_traceability` aplicada ⇒ **20/20 Local = Remote** (chapters/boq_items += `source_code`/`source_row` + CHECK >0; RPC `import_boq_into_version` misma firma persiste code canónico + origen). **RLS runtime 89/89**. Parser propone canónicos genéricos (capítulos dup ⇒ max+1: 7→11/8→12/9→13/10→14; ítems propagan prefijo; ambiguo ⇒ requiresManualReview bloquea); mapping por `rowType:sourceRow`; el navegador solo envía intención (`overrides`), reconstrucción/validación server-side; digest del preview = payload ORIGINAL (integridad). UI "Revisar numeración" editable + Revalidar. 4C.1 (preview/confirm atómico) y 4C.2 (plantilla real) ya desplegadas. **Única acción manual pendiente**: la usuaria sube el Excel ORIGINAL, verifica las sugerencias visibles (7→11…10→14), opcionalmente edita y revalida, y confirma. NO se ha confirmado importación; V01 sigue vacía. **4D NO iniciada.** (Histórico 4C.2/4C.1 y anteriores abajo.)

**ESTADO ACTUAL (2026-06-05) — 4C.2 parser compatible con plantilla real DESPLEGADO:** `main = origin/main = 8a7d4d5` (merge `8a7d4d5`, rama `integration/wave-4c1-excel-import` `a2fb7a6`). Production READY en `construction-ops-psi.vercel.app`. **Fix solo de parser, SIN migración** ⇒ remoto intacto **19/19**. Corregido el fallo del Excel real (`Fila 22: capítulo sin código`): `blankrows:true` (fila real), palabras reservadas (`SUBTOTAL CAPITULO` ignorado, `TOTAL COSTOS DIRECTOS` cierra el BOQ, AIU/control de pagos ignorados), 7 columnas (CAP auxiliar ignorada, ÍTEM=code, VR. PARCIAL=subtotal comparación), diagnóstico AGREGADO (errors[]+warnings[] con fila real, confirmación bloqueada si hay errores), duplicados sin normalización (capítulo=error, ítem=warning). 651 tests, gm 22/22, validador 214/0/0. **Nota de proceso**: el commit `a2fb7a6` quedó por error en `main` local; se reorganizó NO destructivamente (`git branch -f` + merge --no-ff `8a7d4d5`), sin reset --hard ni force-push. NO se confirmó importación (V01 sigue vacía). **Única acción manual pendiente**: la usuaria re-analiza el Excel real desde la UI y comparte solo el resumen/errores de preview. **4C.2 cerrada a falta del smoke; 4D NO iniciada.** (Histórico 4C.1 y anteriores abajo.)

**ESTADO ACTUAL (2026-06-05) — 4C.1 importación de Excel DESPLEGADA:** `main = origin/main = d07e500` (merge `6f536f7`). Production READY en `construction-ops-psi.vercel.app` (`supabase`+`db`). Migración `20260604140000_boq_import_atomic` aplicada ⇒ **19/19 Local = Remote**. RPC `public.import_boq_into_version` `SECURITY INVOKER` (versión vacía/editable `FOR UPDATE` anti doble-submit; subtotal recalculado server-side; grants solo `authenticated`). **RLS runtime 87/87**. Flujo de dos pasos sin persistir archivo: preview (parse SheetJS + digest SHA-256, sin escritura) → confirm (re-parse + compara digest + RPC atómica). Formato congelado v1: `.xlsx`, hoja `COTIZACION 1 PISO`, columnas por encabezado. Límite archivo 3 MB (`serverActions.bodySizeLimit='4mb'` en next.config). Smoke prod: `/login` 200, rutas protegidas 307→/login. **4B.1/4B.2/4B.3 ya cerradas con smoke real.** **Única acción manual pendiente de la usuaria**: subir el Excel real desde PRESUPUESTO BASE → V01 → Importar Excel (analizar → confirmar). Rollback: tag `wave-4b3-real-estimates-production-v1` + `READ_MODEL_SOURCE=fixture`. **4C.2/4D NO iniciadas.** (Histórico 4B.3 y anteriores abajo.)

**ESTADO PREVIO (2026-06-04) — 4B.3 presupuesto inicial + V01 DESPLEGADO:** `main = origin/main = bef4786` (merge `3d031bd`). Production READY en `construction-ops-psi.vercel.app` (`supabase`+`db`). Migración `20260604130000_estimates_authorship_and_atomic_create` aplicada ⇒ **18/18 Local = Remote**. RPC `public.create_estimate_with_initial_version` `SECURITY INVOKER` SIN `p_created_by` (autor derivado de `app._auth_uid()`); estimate `active` + V01 `draft` atómicos; grants solo `authenticated` (revocado anon). RLS runtime **77/77**. Vertical: alcance → sección Presupuestos → `/estimates/new` → `/estimates/[estimateId]` (V01, 0/0, placeholder Excel) → `/estimates` listado real. Smoke prod: `/login` 200, rutas protegidas 307→/login. **4B.1 alcances/4B.2 scopes ya cerrados con smoke real.** **Única acción manual pendiente de la usuaria**: crear `PRESUPUESTO BASE` desde la UI (genera V01). Rollback: tag `wave-4b2-real-scopes-production-v1` + `READ_MODEL_SOURCE=fixture`. **4C (importación Excel) NO iniciada.** (Histórico 4B.2 y anteriores abajo.)

**ESTADO PREVIO (2026-06-04) — 4B.2 alcances reales DESPLEGADOS:** `main = origin/main = 0f470bf` (merge `4e4c74a`). Production READY en `https://construction-ops-psi.vercel.app` (`supabase`+`db`). Migración `20260604120000_project_scopes_authorship` aplicada al remoto ⇒ **17/17 Local = Remote** (única escritura remota; sin seeds/proyectos/alcances remotos creados por terminal). RLS runtime **67/67**. Vertical slice de alcances vivo: `/projects/[id]` sección Alcances + `/scopes/new` + `/scopes/[scopeId]` (request-time, RLS-bound, sin service-role; `scope_type` select default `floor`; `created_by` server-side). Smoke prod: `/login` 200, rutas protegidas 307→/login (sin 500/fixture). **Única acción manual pendiente de la usuaria**: crear el alcance `PRIMER PISO` desde la UI. Rollback estable: tag `wave-4b1-real-projects-production-v1` + `READ_MODEL_SOURCE=fixture`. **4B.3/4C NO iniciadas.** (Histórico abajo.)

**ESTADO ACTUAL (2026-06-04) — 4B.1 estabilización de producción CERRADA:** `main = origin/main = b782502` (merge `c6d0ad1`, docs `b782502`). **Production deploy READY** aliased a `https://construction-ops-psi.vercel.app`. Vercel: scope `soporteatriaworkflows-8854s-projects`, proyecto `construction-ops` (NO `construction-ops-1rqh`); `.vercel` ignorado. **Vercel ahora en `APP_AUTH_MODE=supabase` + `READ_MODEL_SOURCE=db`** (la usuaria activó `db`; `DATABASE_URL` privada en Vercel, nunca pedir/imprimir). Remoto PG17, **16/16**, seeds 0, proyectos 0. Todas las rutas `(dashboard)` son `ƒ` request-time (`force-dynamic` en el layout) con `resolveViewer()`; footer mode-aware (`readModelModeLabel`: db="Datos reales"); CTAs como `Button asChild`+`Link`. Smoke prod: `/login` 200, rutas protegidas 307→/login (sin fixture público, sin 500). **Única acción manual pendiente de la usuaria**: login + crear primer proyecto desde `/projects/new` (listar, abrir detalle). Rollback = `READ_MODEL_SOURCE=fixture` + redeploy. **4B.2/4C NO iniciadas.** (Histórico abajo.)

**CIERRE Oleada 4A.3 (2026-06-02):** autenticación online real **validada en producción**. La usuaria activó manualmente en Vercel `APP_AUTH_MODE=supabase` (+ `READ_MODEL_SOURCE=fixture`) y confirmó el smoke: `/login` → login admin (`GRUPO ICONIC`, rol `admin`) → `/dashboard` → dashboard fixture sanitizado. `/logout` = última comprobación manual de la usuaria. `main = origin/main = cb988be`; tag `wave-4a3-online-auth-validated-v1`. Remoto PG17, 14/14 migraciones, seeds demo 0. **`READ_MODEL_SOURCE=db` NO activado.** Rollback = `demo`+`fixture`+redeploy. **4B (creación real de proyectos con `db`) propuesta pero NO iniciada.**

**Vercel actual a mantener:** `APP_AUTH_MODE=supabase` + `READ_MODEL_SOURCE=fixture` (NO cambiar sin aprobación).

**Hardening `/projects/new` — MERGEADO a `main` (2026-06-04):** merge `0ad7f56` (de `7b112cc`); docs `102462f`. `main = origin/main = 102462f`. Tag `wave-4b1-project-creation-runtime-hardening-v1`. `/projects/new` ahora `async` + `await resolveViewer()` (dinámica intrínseca) + `force-dynamic`; build `ƒ` sin prerender. Solo código (sin migración); remoto intacto 16/16; Vercel sigue `supabase`+`fixture`. Pendiente: la usuaria pone `READ_MODEL_SOURCE=db` + **redeploy SIN Build Cache** + smoke. Hipótesis ambiental (caché obsoleta) aún no confirmada como certeza. (Detalle del diagnóstico abajo.)

**Fix 4B.1 residual `/projects/new` (2026-06-03, rama `fix/wave4b1-runtime-creation-env`, `7b112cc`, mergeado en `0ad7f56`):** force-dynamic solo (en `main` `17b9e07`) NO resolvió el síntoma en prod. **Auditoría profunda probó que el código YA era correcto**: sin literales `process.env.*` (no inlining); chunk compilado lee `a.READ_MODEL_SOURCE`/`a.APP_AUTH_MODE` con `a=process.env` (runtime); `/projects/new` `ƒ` sin prerender ni entrada en `prerender-manifest`; Proxy local leyó `supabase` en runtime. **Causa residual = caché estática/edge OBSOLETA de despliegues previos cuando la ruta era `○` (ambiental, NO código).** Técnica de diagnóstico útil: inspeccionar `.next/server/chunks/*.js` (grep del mensaje + `process.env`/`a.VAR`) y `.next/prerender-manifest.json` para distinguir inlining/prerender de lectura runtime. **Endurecimiento**: `/projects/new` ahora `async` + `await resolveViewer()` (señal dinámica **intrínseca** vía cookies, como `/projects`) + force-dynamic; Vercel no sirve dinámicas intrínsecas desde caché estática ⇒ el deploy supera el HTML obsoleto. **Regla**: rutas cuyo render dependa de env de servidor runtime deben usar señal dinámica intrínseca (cookies/headers), no solo el flag. **Recomendación de deploy: build limpio sin caché + purga de caché del proyecto** antes de reintentar `db`. Validado: 508 tests, build `ƒ`, validador 214/0/0.

**Fix `/projects/new` (force-dynamic) — MERGEADO a `main` (2026-06-03):** merge `1999ffb` (de `9e9dd96`); docs `17b9e07`. `main = origin/main = 17b9e07`. Tag `wave-4b1-project-creation-route-fix-v1`. Build confirma `ƒ /projects/new`. Solo código (sin migración); remoto intacto 16/16; Vercel sigue `supabase`+`fixture`. Pendiente: la usuaria cambia `READ_MODEL_SOURCE`→`db` + redeploy + smoke de creación. (Detalle de causa raíz abajo.)

**Fix 4B.1 `/projects/new` (2026-06-03, rama `fix/wave4b1-project-creation-mode-guard`, `9e9dd96`, mergeado en `1999ffb`):** en modo `db` la página mostraba "Modo demostración activo" porque era **prerenderizada estáticamente** (no usa `cookies()`/`headers()`) y `isCreationModeEnabled()` se evaluaba en build-time con defaults `demo`+`fixture`. Fix: `export const dynamic = 'force-dynamic'` en `apps/web/app/(dashboard)/projects/new/page.tsx` ⇒ request-time (build `○`→`ƒ`). **Regla/aprendizaje:** toda página cuyo render dependa de env de servidor de runtime (`APP_AUTH_MODE`/`READ_MODEL_SOURCE`) debe ser `force-dynamic` o usar una API dinámica; si no, Next la hornea en build. La server action ya guardaba en request-time. Solo código (sin migración). Validado: 507 tests, build, gm 22/22, validador 214/0/0. Pendiente: merge a main + reintento `db` por la usuaria.

**Fix 4B.1 membresía — CERRADO y APLICADO (2026-06-02):** mergeado a `main` (`82c2fa7`; docs `ef995b9`); migración `20260602130000` aplicada al remoto ⇒ **16/16 Local = Remote** (sin seeds, sin proyectos remotos). Tags `wave-4b1-membership-fix-code-ready-v1` / `wave-4b1-membership-fix-remote-ready-v1`. `main = origin/main = ef995b9`. Vercel sigue `supabase`+`fixture`. Próximo paso MANUAL de la usuaria: `READ_MODEL_SOURCE` `fixture`→`db` + redeploy + repetir smoke de creación; rollback = `fixture`+redeploy. 4B.2/4C NO iniciadas. (Detalle de causa raíz/fix abajo.)

**Fix 4B.1 membresía en modo `db` (2026-06-02, rama `fix/wave4b1-membership-resolution`, `ab08b28`, mergeado en `82c2fa7`):** al activar `READ_MODEL_SOURCE=db` en producción, `/projects` daba "El usuario no tiene membresía." Causa raíz DOBLE demostrada local: (#1) las migraciones nunca otorgaron a `authenticated` `USAGE`/`EXECUTE` del esquema `app` — el harness RLS lo daba en runtime (`ensureGrants`), enmascarando; en remoto ⇒ "permission denied for schema app". (#2) `profiles_select` (`organization_id = current_org()`) **recursa** cuando el `SECURITY DEFINER` no salta RLS (postgres remoto SIN `BYPASSRLS`) ⇒ "stack depth limit exceeded". **Regla aprendida:** todo esquema custom (p. ej. `app`) requiere `GRANT USAGE`+`EXECUTE` explícito a `authenticated` en una migración; NO asumir que el harness/local lo refleja (local postgres es superusuario y enmascara bypass+grants). Fix: migración `20260602130000` (grants `app` + `profiles_self_select USING id=(SELECT app._auth_uid())`, self-read sin `current_org()`). Harness ya no enmascara grants de `app` y tiene +3 checks. RLS runtime **61/61**. Pendiente: merge + `db push` de la migración + reintento `db` por la usuaria. Usuaria en rollback `supabase`+`fixture`.

**Oleada 4B.1 — CERRADA y MERGEADA a `main` (2026-06-02):** `main = origin/main = 939af74` (merge `10ac567`). Migración `20260602120000_projects_authorship.sql` aplicada al remoto vía `db push --linked` (sin `--include-seed`) ⇒ **15/15 Local = Remote**; seeds NO ejecutados, proyectos remotos NO creados. Tags `wave-4b1-projects-code-ready-v1` (pre-migración) y `wave-4b1-projects-remote-ready-v1` (post). **Vercel sigue `supabase`+`fixture`.** Próximo paso MANUAL de la usuaria: cambiar solo `READ_MODEL_SOURCE` `fixture`→`db` + redeploy + crear primer proyecto real (smoke remoto); rollback = `fixture`+redeploy. **4B.2/4C NO iniciadas.**

**Histórico — Oleada 4B.1 (integración):** rama `integration/wave-4b1-real-projects` (`1f5f908`). Primera capa de **escritura** real: `apps/web/server/projects/` (`ProjectsWriteRepository`: createProject/getProjectById, RLS-bound, **sin service-role**, `code` autogenerado) + migración `created_by`/`description` + UI (server action `createProjectAction` con viewer real server-side, `mode-guard` creación solo `supabase`+`db`, lista/`/projects/new`/`/projects/[id]`). Validado local: typecheck/lint 0, **505 tests**, build, gm 22/22, validador 214/0/0, **RLS runtime 58/58**, smoke HTTP demo+fixture OK. Backups `backup/wave4b1-projects-db` (`3a403bd`) y `backup/wave4b1-projects-ui` (`6f41fd4`). **NO mergeado a main.** Pendiente: smoke interactivo navegador en modo `db` (login real→crear→detalle; los seeds no fijan password) + autorización de merge. Regla clave: la creación de proyectos **solo** opera con `READ_MODEL_SOURCE=db` (en demo/fixture el botón "+ Nuevo proyecto" va deshabilitado). 4C NO iniciada.

---

Tras el cierre de **Oleada 4A** (merge a `main` `de37d15`, tag `wave-4a-auth-local-v1`, 2026-06-02), el sistema tiene auth Supabase SSR + RLS validados **solo en local** (Docker), nunca contra remoto.

**Why:** decisión de proteger producción: la demo pública debe seguir operando sanitizada sin exponer datos ni requerir login mientras la conexión remota se hace de forma controlada y manual.

**How to apply:**
- Vercel debe permanecer en `APP_AUTH_MODE=demo` + `READ_MODEL_SOURCE=fixture` (la usuaria los configura manualmente; no tocar Vercel desde el repo).
- NUNCA `supabase link`/`db push`/deploy ni conectar Supabase remoto sin aprobación explícita.
- La siguiente microfase aprobada es **Oleada 4A.3 — Supabase remoto controlado + login online** (primero `APP_AUTH_MODE=supabase` con `READ_MODEL_SOURCE=fixture`; luego evaluar `READ_MODEL_SOURCE=db`; rollback = volver a demo+fixture). **Oleada 4B NO debe iniciarse** hasta estabilizar el login online.
- Combinación prohibida por contrato: `APP_AUTH_MODE=demo` con `READ_MODEL_SOURCE=db` (lanza `AuthConfigError`).
- Validación local reproducible: `supabase start -x realtime,studio,storage-api,imgproxy,edge-runtime,logflare,vector` + `db reset` (14 migraciones + 4 seeds) ⇒ **RLS runtime 47/47**. Mailpit local en `http://127.0.0.1:54324`. Ver [[project-toolchain-env]].

**Estado 4A.3 — vínculo controlado (2026-06-02, sesión read-only):**
- Supabase CLI **2.102.0**. Login y `supabase link` los ejecuta la USUARIA en su terminal (mi entorno de herramienta es **no-TTY**: el login automático falla con "Cannot use automatic login flow inside non-TTY environments"). Mis comandos read-only heredan el token del Credential Manager del usuario Windows.
- Proyecto remoto: `construction-ops-prod` · **project-ref `jabddbccmhrxztfzpdii`** · org `oxexzrzkzksgwjaihnjf` · región West US (Oregon). `Finished supabase link.` confirmado; no pidió DB password.
- **Remoto VACÍO**: `migration list --linked` muestra 14 locales con columna Remote en blanco ⇒ 0 migraciones aplicadas. Listas para aplicar en orden.
- `supabase/.temp/` (creado por link) está **ignorado** por `.gitignore:24`; árbol sin cambios de proyecto.
- **Mismatch de versión:** remoto Postgres **17.6.1** vs `config.toml` `major_version = 15`. Antes del `db push` real conviene alinear a `major_version = 17` (cambio local, reversible, orchestrator-owned) y re-validar `db reset` en PG17. No bloquea el `--dry-run`.
- **4A.3a COMPLETADA (2026-06-02)** en rama `integration/wave-4a3-remote-bootstrap` (desde `main` `d3617c3`; `main` intacta). Commits `8a76a75` (config.toml `major_version` 15→17 + docs) y `adf6474` (docs resultados). Para mover de PG15→PG17 local hubo que **descartar el volumen local** con `supabase stop --no-backup` (PG15 incompatible con contenedor PG17); luego `start` ⇒ `server_version 17.6`. Revalidado: `db reset` 14+4, **RLS 47/47**, typecheck/lint 0, **452 tests**, build, gm 22/22, gm:import, validate-agents 214/0/0.
- **`db push --dry-run --linked` ejecutado**: lista 14 migraciones en orden, sin seeds, sin aplicar; el link persistió la credencial (no pidió DB password en mi entorno). `migration list --linked` post-dry-run: remoto sigue con **0** aplicadas.
- **4A.3b COMPLETADA (2026-06-02)**: merge `--no-ff` de paridad PG17 a `main` ⇒ **`139dd52`** (tag `wave-4a3-pg17-bootstrap-ready-v1`); `supabase db push --linked` (sin `--include-seed`, una vez) aplicó las **14 migraciones** al remoto ⇒ `migration list --linked` = **14/14 Local = Remote**. Doc commit **`1ce654e`** (tag `wave-4a3-remote-schema-bootstrap-v1`). `main = origin/main = 1ce654e`. El prompt `[Y/n]` del push se auto-confirmó en entorno no-TTY; único aviso `NOTICE pgcrypto already exists` (benigno).
- **Estado remoto actual**: esquema completo (14 migraciones) pero **SIN datos funcionales** (sin org/usuarios reales; seeds NO ejecutados). Vercel sigue `demo`+`fixture`.
- **Pendiente de autorización explícita — Oleada 4A.3c**: crear organización + usuario admin inicial seguro y conectar login online manteniendo `READ_MODEL_SOURCE=fixture`. Sigue prohibido sin OK: seeds/SQL/usuarios remotos, tocar Vercel, `migration repair` automático. **4B NO iniciada.** Rollback ante fallo pre-datos: corregir puntual o recrear proyecto remoto vacío.

**Fix 4A.3c — login online no cableado (2026-06-02, rama `fix/wave4a3-online-login`, commit `834029c`, NO mergeado):**
- La usuaria activó `APP_AUTH_MODE=supabase` + `fixture` en Vercel y el submit de `/login` daba error genérico **sin** request a `/auth/v1/token` ni log de Auth. Luego restauró Vercel a `demo`+`fixture` para mantener la demo viva.
- **Causa raíz (gotcha de Next):** el cliente de navegador llamaba `getPublicSupabaseEnv()` sin args; esa función lee `env.NEXT_PUBLIC_*` de forma **indirecta** (default `env=process.env`). Next/Turbopack solo inyecta en el bundle del navegador las referencias **LITERALES** `process.env.NEXT_PUBLIC_*`. Indirecto ⇒ `undefined` ⇒ `AuthConfigError` lanzado **antes** de `signInWithPassword()`. (Mismo patrón afecta a reset-password; el fix lo cubre.)
- **Regla permanente:** NUNCA llamar `getPublicSupabaseEnv()` sin argumentos desde código de navegador; pasar siempre un objeto con `process.env.NEXT_PUBLIC_*` literales (ver `apps/web/lib/supabase/client.ts`). Lógica de submit ahora en `apps/web/server/auth/login-flow.ts` (`runPasswordLogin`, pura). Ver [[project-toolchain-env]].
- Validación del fix: typecheck/lint 0, **461 tests**, build, gm 22/22, gm:import, validate-agents 214/0/0.
- **MERGEADO A `main` (2026-06-02):** `git merge --no-ff fix/wave4a3-online-login` (`834029c`) → merge `ad8f32b`, sin conflictos; doc commit `32b7937`. **`main = origin/main = 32b7937`**. Tag `wave-4a3-online-login-fix-v1`. Revalidado en `main`: 461 tests, build, gm 22/22, validador 214/0/0. DB remota intacta (14/14); Vercel SIGUE en `demo`+`fixture`.
- **Pendiente (paso MANUAL de la usuaria, fuera de mi entorno):** poner Vercel en `APP_AUTH_MODE=supabase` (manteniendo `READ_MODEL_SOURCE=fixture`), redeploy y probar login online real con el admin de `GRUPO ICONIC`. Rollback = `demo`+`fixture`. **4B NO iniciar.**
