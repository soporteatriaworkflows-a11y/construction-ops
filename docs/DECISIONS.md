# Decisiones del proyecto

| Fecha | Decisión | Razón | Estado |
|---|---|---|---|
| 2026-06-07 | **4E.2A CERRADA en modo "automated-ready" (sin smoke de escritura productiva).** La usuaria decidió **NO** ejecutar el smoke manual de edición sobre el presupuesto productivo ENTRE PATIOS. En su lugar, 4E.2A se verificó mediante **smoke AUTOMATIZADO no destructivo** contra Postgres 17 / Supabase local usando el **repositorio real** (`DbEstimatesWriteRepository`) a través de PostgREST con RLS (JWT de usuario sembrado), sobre datos **sintéticos** locales: editar cantidad/precio + restaurar EXACTO al baseline (subtotal, subtotal de capítulo, costo directo, A/I/U/IVA, indirecto, total general), PATCH-subtotal-only ignorado por el trigger, crear capítulo/ítem manual (origen NULL, sort append, código único), editar importado preservando `source_code`/`source_row`, mover ítem entre capítulos, seguridad (cross-org/fixture/versión emitida), y export payload que refleja la edición. Producción verificada **read-only** (Vercel READY + smoke HTTP de rutas). **No se modificó ENTRE PATIOS ni dato productivo alguno.** Tag `wave-4e2a-manual-boq-editing-automated-ready-v1`. El smoke de **escritura real en producción queda OPCIONAL** para una sesión futura. | La usuaria prefirió no alterar el presupuesto productivo; la verificación automatizada ejercita el mismo código (repos, recálculo server-side, trigger DB, RLS) sin tocar producción, dando confianza de release sin riesgo sobre ENTRE PATIOS. | ✅ Aprobado |
| 2026-06-07 | **4E.1C CERRADA — smoke productivo de branding oficial APROBADO por la usuaria.** Excel y PDF exportados correctamente; logo completo oficial y símbolo visibles; paleta ICONIC correcta (azul dominante, cian acento, **ausencia de dorado**); jerarquía visual aprobada; **total general intacto** ($372.247.170); presentación general aprobada sin observaciones. Tag estable `wave-4e1c-official-iconic-branding-production-v1`. | Validación visual manual de la usuaria en producción confirma que los assets oficiales y la paleta sin dorado son correctos. Se fija un punto estable antes de iniciar edición manual de BOQ. | ✅ Aprobado |
| 2026-06-07 | **4E.2A — migración del invariant de subtotal APROBADA y APLICADA.** La usuaria aprobó la migración mínima con el ajuste `BEFORE INSERT OR UPDATE` (sobre TODO insert/update, para cerrar el `PATCH subtotal`-solo). Migración `20260606120000_boq_items_subtotal_invariant.sql` (función `public.set_boq_item_subtotal()` con `round(NEW.quantity_snapshot × NEW.unit_price_snapshot, 10)` + trigger `boq_items_recompute_subtotal`; sin SECURITY DEFINER, `SET search_path=public`, sin RLS/tablas/columnas/seeds; DOWN documentado). Validada local (db reset PG17, **RLS runtime 106/106** incl. 17 checks 4E.2A) ⇒ `db push --dry-run` = 1 migración esperada, 0 seeds ⇒ `db push --linked` ⇒ **21/21 Local = Remote**. | El editor manual habilita escrituras directas (no solo la RPC de import); el trigger garantiza que ningún cliente autenticado persista un subtotal arbitrario vía PostgREST, compatible con import/exports (mismo `round(,10)`). | ✅ Aprobado |
| 2026-06-07 | **4E.2A — edición manual NO destructiva de BOQ IMPLEMENTADA** (rama `integration/wave-4e2a-manual-boq-editing`). Crear/editar capítulos e ítems; mover ítem entre capítulos de la misma versión; subtotal/costo directo/AIU/total general recalculados server-side (fuente única `aiu-calc`); trazabilidad `source_code`/`source_row` preservada en importados y NULL en manuales; `sort_order` append; RLS-bound sin service-role; fixture solo lectura. 6 métodos nuevos en `EstimatesWriteRepository` (`createEstimateChapter`/`updateEstimateChapter`/`getEditableEstimateChapter`/`createBoqItem`/`updateBoqItem`/`getEditableBoqItem`), validación pura (`boq-validation.ts`), server actions + formularios + 4 rutas nuevas. Deudas registradas: `BOQ_SAFE_DELETE_OR_ARCHIVE`, `BOQ_REORDER`, `BOQ_AUDIT_TRAIL`, `ESTIMATE_VERSIONING`. | Desbloquea la edición segura del presupuesto manteniendo inmutabilidad de versiones emitidas y una sola fuente de verdad financiera. | ✅ Aprobado |
| 2026-06-07 | **Nota de proceso 4E.2A — WIP de seguridad en paralelo (export-legacy) aislado vía `git stash`.** Durante la sesión aparecieron cambios NO commiteados en `apps/web/{app/api/exports/route.ts, modules/exports/types.ts, server/exports/export-service.ts, tests/unit/exports/export-service.test.ts}` (remediación P1-A/M-02: derivar `organizationId` server-side en exports) ajenos a 4E.2A e incompletos (rompían el typecheck/test del módulo legacy). Se preservaron en 2 `git stash` (recuperables) para validar 4E.2A en aislamiento; NO se commitearon en esta rama. | Mantener la rama 4E.2A limpia y evitar mezclar workstreams; el WIP de seguridad lo completa su propia rama (`fix/security-p1a-...`). | ✅ Aprobado |
| 2026-06-06 | 4E.1C: activados logos oficiales GRUPO ICONIC (full/symbol) embebidos por **script reproducible** `embed-iconic-assets.mjs` → `logo-asset.ts` (data URI base64, sin fs runtime). Paleta oficial `ICONIC_EXPORT_PALETTE` (azul `#005DD6`, cian `#00B8FF`, navy `#020148`…); **dorado `#C8A24B` ELIMINADO**. Guía interna en `docs/branding/` (no publicada). | La usuaria entregó assets oficiales y guía visual; el monograma de 4E.1B era temporal y el dorado no correspondía a la marca. Embed por script evita pegar base64 a mano y mantiene serverless-safe. | ✅ Aprobado |
| 2026-06-06 | 4E.1B: branding visual de exports con **fuente única `branding.ts`** (paleta ICONIC HEX/ARGB) y logo **embebido en base64** (`logo-asset.ts`), NO por `fs`/`outputFileTracingIncludes`. Sin asset ⇒ monograma `IC`. | El logo por `fs` desde `public/` dispara el aviso NFT de Turbopack y no es fiable en serverless; el base64 bundled es serverless-safe y mantiene el build limpio. Branding es solo visual: no toca contenido ni finanzas. | ✅ Aprobado |
| 2026-06-05 | 4E.1: export protegido del presupuesto real se construye como **camino nuevo sobre `EstimatesWriteRepository`** (método `getEstimateExportPayload` + servicio `server/estimates/export`), **sin migración**, reutilizando `exceljs`/`@react-pdf/renderer` ya aprobadas. El módulo legacy `modules/exports` (Oleada 3, read-model demo) se deja intacto. | El export debe reflejar el presupuesto persistido (ENTRE PATIOS→V01) y reutilizar el resumen financiero 4D.2; el módulo legacy está atado al read-model demo y a `DEMO_ORGANIZATION_ID`. Sin duplicar lógica financiera. | ✅ Aprobado |
| 2026-06-05 | 4D.2 CERRADA: AIU editable por versión validado en producción (smoke usuaria, total ≈ $372.247.170). Tag `wave-4d2-editable-aiu-production-v1`. Inicia 4E.1 exportación protegida Excel+PDF en `integration/wave-4e1-budget-exports` | AIU persistente/editable confirmado; siguiente bloque entrega documentos del presupuesto real | ✅ Aprobado |
| 2026-05-29 | ORM: Drizzle ORM | Mejor DX con TypeScript, migraciones SQL directas, sin magia | ✅ Aprobado |
| 2026-05-29 | Stack: Next.js 16 + Supabase + Drizzle | Monolito modular, un solo lenguaje, menor complejidad inicial | ✅ Aprobado |
| 2026-05-29 | Licencia: construcción clean-room | No copiar código AGPL. OpenConstructionERP solo como referencia funcional | ✅ Aprobado |
| 2026-05-29 | Grid: AG Grid Community | Sin costo Enterprise, suficiente para grillas de presupuesto | ✅ Aprobado |
| 2026-05-29 | Gantt: Frappe Gantt (MIT) | Zero dependencias, embebible en React, licencia libre | ✅ Aprobado |
| 2026-05-29 | Gestor de paquetes: **pnpm** (workspace) | Lockfile único `pnpm-lock.yaml`; no mezclar npm/yarn; no generar `package-lock.json` | ✅ Aprobado |
| 2026-05-29 | pnpm fijado vía Corepack → `packageManager: pnpm@11.5.0` | Reproducibilidad; `latest-11` resolvió a 11.5.0 | ✅ Aprobado |
| 2026-05-29 | Runtime: Node.js 24 (v24.13.0 detectado) | Objetivo Node 24 LTS; no se cambió globalmente la versión | ✅ Aprobado |
| 2026-05-29 | Test runner: Vitest | Rápido, ESM-first, integra con el stack TS | ✅ Aprobado |
| 2026-05-29 | `drizzle-kit` diferido (no instalado en Paso 0) | No necesario para typecheck/lint/test/build; lo solicitará agent-db-rls vía INTEGRATION_REQUESTS | ✅ Aprobado |
| 2026-05-29 | Aprobación de builds pnpm 11 vía `allowBuilds` (mapa `pkg: bool`) | Clave vigente en pnpm 11.5.0 (verificado en su dist, 91 ocurrencias). Se eliminó `onlyBuiltDependencies` (legacy). Builds aprobados: esbuild, sharp, unrs-resolver | ✅ Aprobado |
| 2026-05-29 | Upgrade a **Next.js 16.2.6 + React 19.2.6** | Migrar antes de tener funcionalidad real; usar estable no-canary (canary 16.3.0 descartado) | ✅ Aprobado |
| 2026-05-29 | Convención de red: **`proxy.ts`** (no `middleware.ts`) | Next 16 deprecó/renombró `middleware` → `proxy`. `middleware.ts` eliminado | ✅ Aprobado |
| 2026-05-29 | Lint: **ESLint 9 flat config** (`eslint.config.mjs`), `eslint .` | Next 16 eliminó `next lint`; se consume el flat config de `eslint-config-next` 16. `.eslintrc.json` eliminado | ✅ Aprobado |
| 2026-05-29 | TypeScript fijado en `^5.9.3` y ESLint en `^9.39.4` | Estables maduros; se descartan TS 6.0.3 y ESLint 10.4.1 por recién liberados | ✅ Aprobado |
| 2026-05-29 | `AGENTS.md` + `@AGENTS.md` en CLAUDE.md | Obligar a consultar la doc versionada de Next en `node_modules/next/dist/docs/` | ✅ Aprobado |
| 2026-05-29 | **Contrato de entidades congelado v1** (DATABASE_SCHEMA + API_CONTRACTS) | Fuente única de verdad para paralelizar Oleada 1 sin divergencias. 20 entidades congeladas + 7 provisionales v0 | ✅ Aprobado |
| 2026-05-29 | DB `snake_case` ↔ TS `camelCase`; tipos `PascalCase` | Convención uniforme entre capas | ✅ Aprobado |
| 2026-05-29 | Dinero como **`string` decimal** en API; `NUMERIC(20,10)` en DB; Decimal.js para operar | Preservar precisión COP; el frontend NO calcula totales financieros | ✅ Aprobado |
| 2026-05-29 | Snapshots y versiones emitidas inmutables (RLS bloquea UPDATE/DELETE) | No recalcular presupuestos emitidos | ✅ Aprobado |
| 2026-05-29 | Privacidad **backend-first** (campos 🔒 no se serializan a rol cliente) | No basta ocultar en UI; el backend omite los campos internos | ✅ Aprobado |
| 2026-05-29 | Alcance Oleada 1 = solo entidades congeladas v1 | Vertical de presupuesto; planning/ejecución/compras/actas diferidas | ✅ Aprobado |
| 2026-05-29 | `.worktreeinclude` limitado al golden master exacto | Los worktrees aislados no copian archivos ignorados por Git; `agent-excel-mapper` necesita el Excel privado. **TEMPORAL Oleada 1**: revisar/retirar tras crear el fixture sanitizado. NO incluye toda `private/` ni `.env` | ✅ Aprobado |
| 2026-05-29 | Dependencias Oleada 1 instaladas (pnpm) | db: drizzle-kit (raíz), postgres. excel/decimal: xlsx, decimal.js. frontend: ag-grid-community/react, clsx, tailwind-merge, cva, lucide-react, @radix-ui/react-slot. Todas permisivas, sin AGPL | ✅ Aprobado |
| 2026-05-29 | Diferidas: recharts, frappe-gantt, exceljs, @react-pdf/renderer | No necesarias hasta Oleada 3 (dashboard/planning/exports); se pedirán vía INTEGRATION_REQUESTS | ✅ Aprobado |
| 2026-05-29 | `drizzle-kit` en `package.json` raíz (no en apps/web) | `drizzle.config.ts` vive en raíz; el CLI se ejecuta desde ahí (`pnpm db:generate`) | ✅ Aprobado |
| 2026-05-30 | `tsx` (devDep raíz) + scripts `gm:dump`/`gm:build-fixture`/`gm:regression`/`gm:import` | Validación empírica del golden master (Oleada 1, Fase 1). `tsx` ejecuta scripts TS del importador | ✅ Aprobado |
| 2026-05-30 | Fixture regenerado **fila por fila** desde el Excel real (v2.0.0) | 14 capítulos + 131 ítems BOQ reales; SIN ítem de balanceo artificial; Σ ítems = costos_directos ±2e-8 COP | ✅ Aprobado |
| 2026-05-30 | `findPrivateLeaks` corregido (escanea solo texto libre) | Evita falsos positivos sobre UUIDs/DecimalString; los datos privados viven en texto, no en números | ✅ Aprobado |
| 2026-05-30 | Integración Oleada 1 en rama `integration/wave-1` (NO merge directo a main) | Proteger `main`; validar los 3 entregables en conjunto antes del merge | ✅ Aprobado |
| 2026-05-30 | `drizzle-orm` alineado a `^0.45.2` | El schema de db-rls usa la API moderna (callback de tabla retorna array); 0.33 esperaba objeto. 0.45 empareja con drizzle-kit 0.31 | ✅ Aprobado |
| 2026-05-30 | Fix tipos AG Grid v35 (`boq-grid.tsx`) + orden `@import` en `globals.css` | `getRowStyle`→`RowClassParams/RowStyle`; `ColDef[]` con cast localizado; `@import` AG Grid antes de `@tailwind` (build Turbopack) | ✅ Aprobado |
| 2026-05-30 | Regex de tests RLS acotados por política | Evitar `[\s\S]*?` greedy que cruza tablas (falsos positivos). Migraciones NO modificadas | ✅ Aprobado |
| 2026-05-30 | `.worktreeinclude` ELIMINADO (Fase 6) | Fixture sanitizado fila-por-fila suficiente; Oleada 2 consume el fixture, no el Excel. `private/` sigue ignorado | ✅ Aprobado |
| 2026-05-30 | **Merge de Oleada 1 a `main`** (`--no-ff`, merge commit `58f4366`) | Aprobado por el usuario. Post-merge PASS (tc/lint/test 108/build/gm 22/gm:import/validador 214). `integration/wave-1` y backups conservados. Tag `wave-1-foundation-v1` | ✅ Aprobado |
| 2026-05-30 | **Q8 RESUELTA — Base del descuento = `online_public_price`** | `negotiated_discount_pct` se aplica por defecto sobre `online_public_price`, NO sobre `budget_reference_price`. Fórmulas canónicas: `budget_reference_price = online_public_price × (1 + preventive_variation_pct)`; `expected_purchase_price = online_public_price × (1 − negotiated_discount_pct)`; `projected_saving = budget_reference_price − expected_purchase_price`; `realized_saving = budget_reference_price − actual_purchase_price`. Excepciones futuras configurables por proveedor/producto. Descuento, ahorro y margen son internos (🔒), nunca al rol cliente. Privacidad backend-first. Cerrada en Oleada 1.5 (rama `feature/wave-1.5-local-rls`) | ✅ Aprobado |
| 2026-05-30 | **Supabase CLI `supabase ^2.102.0`** (devDep raíz, MIT) | Validar RLS runtime contra PostgreSQL **local** (Docker): `supabase start` + `db reset`. NO `link`, NO `db push`, NO remoto. Sin instalación global. Oleada 1.5 | ✅ Aprobado |
| 2026-05-30 | **Q9 RESUELTA — Redondeo COP: cálculo raw / presentación `ROUND_HALF_UP`** | Cálculo interno: `Decimal.js`, persistir `NUMERIC(20,10)`, serializar dinero como `string` decimal, sin float JS, sin redondear pasos intermedios, snapshots con precisión completa. Presentación: `ROUND_HALF_UP`; UI/PDF cliente COP sin decimales; Excel técnico interno hasta 2 decimales; regresión/auditoría precisión raw completa. El redondeo visual NO modifica snapshots, cálculos ni regresión. Separación explícita cálculo↔presentación. Cerrada en Oleada 1.5 | ✅ Aprobado |
| 2026-05-30 | **Merge de Oleada 1.5 a `main`** (`--no-ff`, merge commit `1ddc833`; docs `974ea99`) | Aprobado por el usuario. RLS runtime 21/21; Q8/Q9 cerradas; post-merge PASS (tc/lint/test 108/build/gm 22/gm:import 9/validador 214). Tag `wave-1.5-rls-runtime-validated-v1`. Backups y `feature/wave-1.5-local-rls` conservados | ✅ Aprobado |
| 2026-05-30 | **B-004 — Realtime unhealthy en Docker Desktop (Windows)** como deuda técnica no bloqueante | No afecta RLS (solo se requiere el contenedor `db`); RLS runtime 21/21. Revisar antes de funcionalidades Realtime o producción | ✅ Aprobado |
| 2026-05-30 | **Oleada 2A sobre rama `integration/wave-2a`** (NO merge directo a main) | Proteger `main`; cost-domain ∥ pricing en worktrees; integración y validación conjunta antes del merge | ✅ Aprobado |
| 2026-05-30 | **Contrato de lectura de precios congelado v1** (`docs/PRICING_READ_CONTRACT.md`) | Desacopla cost-domain (consume `PricingReadPort`/`ApprovedPriceContext`) de pricing (lo implementa). Permite paralelizar Oleada 2A sin bloqueo. Fórmulas Q8 base `onlinePublicPrice`; dinero/porcentajes `DecimalString`; privacidad backend-first (`ClientSafePrice`). Cambios solo vía INTEGRATION_REQUESTS | ✅ Aprobado |
| 2026-05-30 | **Integración Oleada 2A en `integration/wave-2a`** (cherry-picks `6694d88` cost-domain + `1e8a869` pricing; sin merge a `main`) | Validar conjuntamente motor financiero + pricing antes de pedir merge. `main` intacta | ✅ Aprobado |
| 2026-05-30 | **Fuente única de código del contrato de lectura**: `apps/web/lib/contracts/pricing-read.ts` | Elimina la duplicación de `PricingReadPort`/`ApprovedPriceContext` (cost-domain y pricing lo implementaron por separado). cost-domain importa; pricing implementa. Forma canónica = la del contrato congelado (async, `PricingReadResult` union, contexto completo) + clases de error `ApprovedPriceNotFoundError`/`AmbiguousApprovedPriceError`. cost-domain pasó a async | ✅ Aprobado |
| 2026-05-30 | **Ajuste menor del contrato**: `PricingReadQuery.estimateVersionId?` opcional | cost-domain congela el precio por versión de presupuesto; pricing puede ignorarlo (resuelve por `asOf`). Compatibilidad semántica preservada; documentado en PRICING_READ_CONTRACT y API_CONTRACTS | ✅ Aprobado |
| 2026-05-30 | **Base del IVA derivada de `base_type` + `sortOrder`** (sin flag `contributesToUtilityBase`) | Una regla `base_type='utility'` se aplica sobre el monto de la última línea `direct_cost` precedente (la Utilidad). Una sola fuente de verdad (esquema); reproduce el golden master (IVA = Utilidad × 0.19, diff=0). Sin cambios al seed/fixture | ✅ Aprobado |
| 2026-05-30 | **Merge de Oleada 2A a `main`** (`--no-ff`, merge commit `f0c7d23`) | Aprobado por el usuario. Post-merge PASS (tc/lint 0, **test 229**, build, gm:regression 22/22, gm:import 9/9 diff=0, validador 214/0/0). Fuente única `apps/web/lib/contracts/pricing-read.ts`; IVA por `base_type='utility'`. Sin cambios de DB ⇒ RLS runtime 21/21 vigente. Backups (wave1+wave2) e `integration/wave-2a` conservados. Tag `wave-2a-domain-pricing-v1` | ✅ Aprobado |
| 2026-05-30 | **Q11 RESUELTA — Aprobación humana de importación/precios: SIMPLE en MVP** | Aprobación por un usuario interno autorizado; auditoría obligatoria (aprobador, timestamp, fuente, motivo, override, observación previa, resultado, `supplierProductId`, `resourceId`, `observedAt`, `sourceType`, `sourceReference`). Importación NO persiste automáticamente (preview primero); SKU ambiguos `pending`; no se tocan snapshots emitidos; `price_observations` append-only. Doble aprobación = soporte futuro configurable (umbral/anomalía/proveedor crítico/organización/rol). Oleada 2B | ✅ Aprobado |
| 2026-05-30 | **Q14 RESUELTA — Canal Homecenter: adaptador genérico + CSV/Excel en MVP** | Implementación inicial por archivo CSV/Excel; preview obligatorio; aprobación humana antes de persistir; SKU/URL opcionales; matching con candidatos y score; fallback manual; trazabilidad. NO asumir API pública/feed/endpoints/acceso empresarial. Prohibido scraping agresivo, automatización opaca, tocar presupuestos emitidos, persistir ambiguos como aprobados. Interfaz sustituible (API/feed/cotización/manual/n8n supervisado). Oleada 2B | ✅ Aprobado |
| 2026-05-30 | **Contrato del adaptador de proveedores congelado v1** (`docs/PRICING_ADAPTER_CONTRACT.md`) | `agent-homecenter` implementa `apps/web/modules/pricing/adapters/` + `scripts/catalog-sync/`; consume `PricingApprovalPort`; no calcula APU/BOQ/AIU ni define capas de pricing. Interfaz `SupplierAdapter` + tipos `RawSupplierItem`/`SkuMatchProposal`/`ImportPreview`/`ImportResult`; idempotencia; privacidad backend-first (SKU/URL/`sourceReference`/proveedor/precio público/candidatos/score/aprobador/motivos = 🔒). Cambios solo vía INTEGRATION_REQUESTS | ✅ Aprobado |
| 2026-05-30 | **Integración Oleada 2B en `integration/wave-2b`** (cherry-pick `1a8f6fa` del adaptador; sin merge a `main`) | Reconciliación + validación conjunta antes de pedir merge. `main` intacta | ✅ Aprobado |
| 2026-05-30 | **`MinimalApprovalPort` = subconjunto estructural de `PricingApprovalPort`** (no lógica paralela) | El adaptador delega la persistencia en el puerto real (`PricingApprovalService`). Prueba `port-reconciliation.test.ts`: aserción type-level + runtime con el servicio real (append-only, idempotencia). `ResourceCatalog`/`ResourceRef` se avalan como tipos auxiliares de matching dentro de `adapters/` | ✅ Aprobado |
| 2026-05-30 | **Merge de Oleada 2B a `main`** (`--no-ff`, merge commit `47fcfb3`) | Aprobado por el usuario. Adaptador Homecenter CSV/Excel (preview, matching SKU, aprobación humana Q11, idempotencia) vía `PricingApprovalPort` real; sin scraping; sin API inventada. Post-merge PASS (tc/lint 0, **test 309** [80 adapters], build, gm 22/22 + 9/9, validador 214/0). Sin cambios de DB ⇒ RLS runtime 21/21 vigente. Backups (6) e `integration/wave-2b` conservados. Tag `wave-2b-homecenter-adapter-v1` | ✅ Aprobado |
| 2026-05-31 | **Oleada 3A — read-model congelado v1** (`docs/READ_MODEL_CONTRACT.md`) | Capa server-side única (`apps/web/server/read-model`) que alimenta las pantallas sin recalcular finanzas ni exponer 🔒. `ReadModelPort` + DTOs en `apps/web/lib/contracts/read-model.ts`. Dos fuentes (`FixtureReadModelRepository`/`DrizzleReadModelRepository`) vía `READ_MODEL_SOURCE=fixture\|db`, sin fallback silencioso. Proyección por `ViewerRole`. Orden 3A→3B→3C. Cambios solo vía INTEGRATION_REQUESTS | ✅ Aprobado |
| 2026-05-31 | **`recharts` ^3.8.1 (MIT)** instalado en `apps/web` | Gráficos del dashboard (Oleada 3A). Solo frontend. NO se instalaron frappe-gantt/exceljs/@react-pdf/renderer (diferidas a 3B/3C). Licencia permisiva verificada | ✅ Aprobado |
| 2026-05-31 | **Integración Oleada 3A en `integration/wave-3a`** (sin merge a `main`) | db-rls (read-model) ∥ frontend-boq (páginas) ∥ dashboard (KPIs) en worktrees; integración y validación conjunta antes del merge. `main` intacta | ✅ Aprobado |
| 2026-05-31 | **Read-model canónico cableado a la UI** (cherry-picks `9e69449`/`8ab0d9c`/`0bab01d`) | Una sola `apps/web/lib/contracts/read-model.ts`; páginas y dashboard consumen `getReadModel()` de `@/server/read-model`; accesores TEMP eliminados; **`getDemoViewer()`** para preview fixture (no auth). Páginas adaptadas a DTOs canónicos. Dev smoke 8/8 HTTP 200 con datos reales del golden master. typecheck/lint 0, 356 tests, build OK, gm 22/22 + 9/9, validador 214/0. Sin cambios de DB ⇒ RLS 21/21 vigente | ✅ Aprobado |
| 2026-05-31 | **Merge de Oleada 3A a `main`** (`--no-ff`, merge commit `d1f0920`) | Aprobado por el usuario. Web local funcional con read-model + fixture sanitizado. Post-merge PASS (tc/lint 0, **356 tests**, build, gm 22/22 + 9/9, validador 214/0). 1 `ReadModelPort`/`getReadModel()`; sin dev-read-model; sin imports de mocks activos. Sin cambios de DB ⇒ RLS 21/21 vigente. `getDemoViewer()` solo demo/dev — auth real/sesión es prerrequisito del modo `db`. Backups (9) e `integration/wave-3a` conservados. Tag `wave-3a-functional-read-model-ui-v1` | ✅ Aprobado |
| 2026-05-31 | **Oleada 3B — PLANNING_CONTRACT congelado v1** (`docs/PLANNING_CONTRACT.md`) | Esquema nuevo (`schedule_tasks`/`task_dependencies`/`progress_entries`/`resource_assignments` con RLS); dominio puro `apps/web/modules/planning/` (CPM/ruta crítica/holguras/ciclos, `DecimalString`); extensión read-model (`getSchedule`/`listProgressEntries`/`listResourceAssignments`). `progress_entries` append-only; avance financiero/holguras/ruta crítica server-side (cero en React); privacidad por rol; campos reservados MS Project (export en 3C). Cambios solo vía INTEGRATION_REQUESTS | ✅ Aprobado |
| 2026-05-31 | **`frappe-gantt` ^1.2.2 (MIT)** instalado en `apps/web` | Vista Gantt de planning (Oleada 3B). Solo frontend; import dinámico (DOM/SVG). NO se instalaron exceljs/@react-pdf/renderer (diferidas a 3C). Licencia permisiva verificada | ✅ Aprobado |
| 2026-05-31 | **Integración Oleada 3B en `integration/wave-3b`** (sin merge a `main`) | db-rls (esquema/RLS/read-model planning) ∥ planning (dominio/Gantt) en worktrees; integración y validación conjunta antes del merge. `main` intacta | ✅ Aprobado |
| 2026-06-01 | **Continuación secuencial 3B** (db `560b2cc` → ui `41abbe2`) por límite de sesión | Los subagentes paralelos quedaron incompletos (interrumpidos); se completaron **secuencialmente** desde los WIP preservados (`89bfab7`/`6f4dab9`) vía `git merge --ff-only` (sin redo). db: 3 métodos read-model + seed + RLS runtime 32/32. ui: página `/planning` + Gantt + tests; reconciliación `view-model`→`ScheduleSummary` canónico; CSS frappe-gantt vendorizado | ✅ Aprobado |
| 2026-06-01 | **Integración formal 3B en `integration/wave-3b`** (`git merge --no-ff continuation/wave3b-planning-ui`, merge commit `595e5a5`) | Aprobada por el usuario. Sin conflictos. Read-model único; `/planning` consume `getReadModel().getSchedule()` real (sin TEMP); **CPM/holguras solo en `modules/planning`** (cero en React); frappe-gantt dinámico; nav `/planning`. Validado: typecheck/lint 0, **389 tests**, build (`/planning`), gm 22/22 + 9/9, validador 214/0, **RLS runtime 32/32** (24 tablas FORCE), dev smoke **9/9 HTTP 200**. Sin merge a `main` | ✅ Aprobado |
| 2026-06-01 | **Merge de Oleada 3B a `main`** (`--no-ff`, merge commit `40118a5`) | Aprobado por el usuario. Sin conflictos (44 archivos, +5221/-11). Planning completo: esquema (4 tablas) + RLS + read-model + dominio CPM/holguras + `/planning` + Gantt + avance append-only. Post-merge PASS: typecheck/lint 0, **389 tests**, build Next 16.2.6 (`/planning`), gm 22/22 + 9/9, validador 214/0, **RLS runtime 32/32** (13 migraciones + 3 seeds, **24 tablas FORCE**), dev smoke **9/9 HTTP 200** (sin fugas internas; rol `management` ve Holgura/Crítica). Read-model canónico único; CPM solo en `modules/planning`; frappe-gantt dinámico + CSS vendorizado. **B-005** (espejo DTO interno en `modules/planning/types.ts`, campo muerto `financialProgressPct`) registrada como deuda no bloqueante para limpieza en 3C. Backups/continuations e `integration/wave-3b` conservados. Tag `wave-3b-planning-gantt-v1` | ✅ Aprobado |
| 2026-06-01 | **Oleada 3C sobre rama `integration/wave-3c`** (NO merge directo a main) | Exportaciones Excel/PDF/CSV. Ejecución **secuencial** (1 agente: `agent-exports`); proteger `main`; integración y validación conjunta antes del merge | ✅ Aprobado |
| 2026-06-01 | **B-005 RESUELTO — Espejo DTO de planning plegado a fuente única** (`integration/wave-3c`) | `modules/planning/types.ts` re-exporta los DTOs/enums (`ScheduleTaskView`/`DependencyView`/`MilestoneView`/`ProgressEntryView`/`ResourceAssignmentView`/`CriticalPathSummary`/`ScheduleTaskStatus`/`DependencyType`) desde el contrato canónico `apps/web/lib/contracts/read-model.ts` y conserva solo tipos de dominio puro; eliminado el campo muerto `financialProgressPct` y `ScheduleSummaryView` sin uso. Superficie pública de `@/modules/planning` estable. Sin cambios en CPM/holguras/proyección/DB/migraciones/seeds/RLS. Validación: typecheck/lint 0, 389 tests, build (`/planning`), gm 22/22 + 9/9, diff limpio; cero regresiones (1 archivo, 252→176 líneas). Commit `refactor(planning): fold duplicated DTO mirrors into canonical read model` | ✅ Aprobado |
| 2026-06-01 | **Oleada 3C — EXPORT_PROFILES_CONTRACT congelado v1** (`docs/EXPORT_PROFILES_CONTRACT.md`) | 4 perfiles (`client`/`site`/`management`/`internal` = `ViewerRole`); whitelist por perfil; 5 formatos MVP (`xlsx-client`/`xlsx-internal`/`pdf-client`/`pdf-management`/`csv-schedule`); contratos `ExportRequest`/`ExportFormat`/`ExportResult`/`ExportService`. Proyección **server-side** antes de serializar; consume DTOs del read-model canónico; **sin** recálculo financiero, **sin** tablas crudas, **sin** float dinero (Q9), **sin** `.mpp`/XML MS Project. Cambios solo vía INTEGRATION_REQUESTS | ✅ Aprobado |
| 2026-06-01 | **`exceljs` ^4.4.0 (MIT)** instalado en `apps/web` | Generación XLSX de exportaciones (Oleada 3C). Solo server-side. Transitiva `jszip` doble `MIT OR GPL-3.0-or-later` → se elige **MIT** (no AGPL, no contamina). Escaneo sin AGPL/enterprise. Licencia permisiva verificada | ✅ Aprobado |
| 2026-06-01 | **`@react-pdf/renderer` ^4.5.1 (MIT)** instalado en `apps/web` | Generación PDF de exportaciones (Oleada 3C). Solo server-side; peer React ^19 (compatible). Árbol transitivo sin AGPL ni enterprise; fuentes por defecto (sin fuentes privadas). Licencia permisiva verificada | ✅ Aprobado |
| 2026-06-01 | **Divergencia `origin/main` por PR de GitHub (SQUASH) — reconciliada (Caso B)** | El PR "Integration/wave 3c (#1)" se fusionó en GitHub como **squash** ⇒ `origin/main = b09158f` (padre único `82f9e86`); `2621d3b` **NO** quedó como ancestro. `origin/main` ya contenía **materialmente** B-005 + `EXPORT_PROFILES_CONTRACT` + deps (contenido idéntico), faltando solo el código de exports. Estrategia: rama `integration/wave-3c-main-reconciled` desde `origin/main` + `git merge --no-ff backup/wave3c-exports` → **solo 15 archivos de exports** añadidos, **sin conflictos** ni duplicación (B-005/contrato/deps idénticos auto-resueltos como no-op). Sin force-push, sin rebase, sin reset | ✅ Aprobado |
| 2026-06-01 | **Merge de Oleada 3C (exports) a `main`** (`--no-ff`) | Aprobado por el usuario. `integration/wave-3c-main-reconciled` → `main`. 5 formatos (xlsx-client/internal, pdf-client/management, csv-schedule) × 4 perfiles; `ExportService` consume DTOs proyectados (0 tablas crudas, 0 recálculo, Decimal Q9); `/api/exports`. Post-merge PASS: typecheck/lint 0, **410 tests**, build (`/api/exports`), gm 22/22 + 9/9, validador 214/0, **smoke 6/6 HTTP 200** + negativos 400, privacidad por perfil. RLS runtime 32/32 vigente (sin cambios DB). Sin `.mpp`/XML MS Project. Backups/continuations e `integration/wave-3c` conservados. Tag `wave-3c-exports-v1`. **Oleada 3C CERRADA** | ✅ Aprobado |
| 2026-06-01 | **Oleada 4A.1 sobre rama `integration/wave-4a-auth-local`** (NO merge directo a main) | Fundamento local de auth/RLS real. Ejecución **secuencial** (único agente: `agent-db-rls`); proteger `main`; sin remoto/UI en esta microfase | ✅ Aprobado |
| 2026-06-01 | **Oleada 4A — AUTH_CONTRACT congelado v1** (`docs/AUTH_CONTRACT.md`) | Modos `APP_AUTH_MODE` (`demo`\|`supabase`) × `READ_MODEL_SOURCE` (`fixture`\|`db`) sin fallback silencioso (prod real ≠ demo); `AuthenticatedViewer` resuelto server-side desde sesión→`profiles` (membresía single-org **reutilizada**, sin tablas nuevas); org/rol nunca desde el navegador; deny-by-default; mapeo `profiles.role`→`ViewerRole` (admin/gerencia→management, presupuestos/compras→internal, obra→site, consulta→client); matriz ruta×rol; rutas públicas/protegidas; helpers SQL deben resolver `auth.uid()`→`profiles` con compat demo. Cambios solo vía INTEGRATION_REQUESTS | ✅ Aprobado |
| 2026-06-01 | **`@supabase/supabase-js` ^2.106.2 (MIT)** + **`@supabase/ssr` ^0.10.3 (MIT)** instalados en `apps/web` | Auth real (Oleada 4A): cliente oficial + sesión SSR por cookies. Peer `supabase-js ^2.105.3` cumplida; compatible Next 16. Sin AGPL/enterprise. `service_role` NUNCA en frontend; clave publishable/anon es pública. Licencias permisivas verificadas | ✅ Aprobado |
| 2026-06-01 | **Integración Oleada 4A.1 (auth/RLS DB) en `integration/wave-4a-auth-local`** (`--no-ff`, merge `adeafbe`) | Aprobada por el usuario. Sin conflictos (4 archivos, +401). Migración `20260601090000_auth_identity_helpers.sql`: helpers `app.current_org()/current_role()/current_org_user()` resuelven identidad real (`auth.uid()`→`profiles`) con **COALESCE** a claims demo (compat); `_jwt_claims()`/`_auth_uid()`/`_profile_org()/_profile_role()` (SECURITY DEFINER + search_path fijo, sin recursión RLS); **deny-by-default**. Seed `0004` (org B + roles + usuario sin membresía, sanitizado). **Reutiliza `profiles` single-org v1, sin tablas nuevas.** Validado: **RLS runtime 47/47** (32 previos + 15 auth), typecheck/lint 0, **410 tests**, build, gm 22/22 + 9/9, validador 214/0; sin secretos. `main` intacta; remoto no conectado. Sesión SSR/viewer real/proxy/UI → 4A.2. **Sin merge a `main`** | ✅ Aprobado |
| 2026-06-01 | **Oleada 4A.2 sobre rama `integration/wave-4a-auth-runtime`** (desde 4A.1, NO merge a main) | Runtime SSR + UI auth. Secuencial: orquestador implementa runtime; luego `agent-frontend-boq` la UI | ✅ Aprobado |
| 2026-06-01 | **AUTH_RUNTIME_CONTRACT congelado v1** (`docs/AUTH_RUNTIME_CONTRACT.md`) | Suplemento de runtime sobre AUTH_CONTRACT: matriz `APP_AUTH_MODE`×`READ_MODEL_SOURCE` (incl. `supabase+fixture` para smoke) sin fallback; mapeo role→ViewerRole **fuente única** en `server/auth/role-map.ts` (**admin→internal**); rutas públicas/protegidas; redirecciones; sanitización de `next` (anti open-redirect); SSR oficial `@supabase/ssr` (`getAll`/`setAll`, `getClaims()` como guard, **prohibido** `getSession()` server-side y `auth-helpers-nextjs`); `/api/exports` exige viewer y **prohíbe escalamiento** de perfil (≤ ViewerRole). Cambios solo vía INTEGRATION_REQUESTS | ✅ Aprobado |
| 2026-06-01 | **Runtime SSR implementado** (`feat(auth)`, rama 4A.2) | `lib/supabase/{env,client,server,proxy}.ts`, `server/auth/{types,errors,role-map,session,resolve-viewer,routes,index}.ts`, `apps/web/proxy.ts` (guard + refresh, demo passthrough), `app/page.tsx` (→`/dashboard`), guard de `/api/exports` (modo supabase: viewer + anti-escalamiento). `resolveViewer()` por modo, sin fallback silencioso. Validado: typecheck/lint 0, **423 tests** (+13 auth), build (`/api/exports` + Proxy), gm 22/22 + 9/9, validador 214/0. Sin secretos; `READ_MODEL_SOURCE=fixture` por defecto | ✅ Aprobado |
| 2026-06-02 | **UI auth 4A.2 recuperada tras apagado + fix Suspense `/login`** (`backup/wave4a-auth-ui` `c9063ad`) | `agent-frontend-boq` dejó la UI (`(auth)/login\|forgot-password\|reset-password\|logout\|auth/callback`, `components/auth/*`, `tests/unit/auth-ui/*`) **sin commitear** en su worktree; recuperada y preservada. Build de producción exigió **wrap de `useSearchParams` en `<Suspense>`** en `/login` (CSR bailout Next 16): extraído `LoginForm` + `<Suspense>`. Validado en worktree: typecheck/lint 0, **452 tests**, build OK | ✅ Aprobado |
| 2026-06-02 | **Integración UI auth 4A.2 en `integration/wave-4a-auth-runtime`** (`git merge --no-ff backup/wave4a-auth-ui`, merge `5c60339`) | Aprobada por el usuario. **Sin conflictos** (11 archivos, +1106). HANDOFF_LOG resuelto 3-way (versión runtime). Post-merge PASS: typecheck/lint 0, **452 tests**, build (Proxy + rutas auth + `/api/exports`), gm 22/22 + 9/9, validador 214/0; diff limpio; Excel ignorado; sin `.env`/`.env.local`/lock/privados. **NO merge a `main`** | ✅ Aprobado |
| 2026-06-02 | **Smoke local end-to-end (Supabase local, Docker)** — auth real validada | `supabase start -x realtime,studio,storage-api,imgproxy,edge-runtime,logflare,vector` + `db reset` (14 migraciones + 4 seeds limpios) + **RLS runtime 47/47**. `.env.local` (ignorado): `APP_AUTH_MODE=supabase`, `READ_MODEL_SOURCE=fixture`, URL/publishable local. Usuarios seed reutilizados con contraseña **efímera local** (no en repo). Smoke: `/login` 200; `/dashboard` sin sesión→`/login?next=/dashboard`; credenciales inválidas→error legible; login admin→sesión+cookies; `/dashboard` auth 200; `/login` auth→`/dashboard`; `/api/exports` sin sesión→bloqueado (Proxy 307); **anti-escalamiento** client→internal/management **403**, client→client 200; export autorizado 200 (PDF `%PDF`); logout→cookie borrada+`/login`; forgot→recover 200 + **Mailpit** "Reset Your Password"; callback sin code→`/login?error`. **Modo demo** (`APP_AUTH_MODE=demo`): `/dashboard` 200 sin Supabase. Supabase remoto NO conectado; Vercel NO modificado (demo fixture pública intacta) | ✅ Aprobado |
| 2026-06-02 | **Merge de Oleada 4A a `main`** (`--no-ff`, merge commit `de37d15`) | Aprobado por el usuario. `integration/wave-4a-auth-runtime` (`cc61eec`) → `main`, **sin conflictos** (44 archivos, +3185/-133). Incluye DB/RLS 4A.1 (migración `auth_identity_helpers` + seed `0004`, identidad por `auth.uid()`→`profiles`, single-org v1, sin tablas nuevas), runtime SSR (`@supabase/ssr`, `getAll/setAll`, Proxy Next 16 con `getClaims()` sin `getSession()`, viewer real, role-map, anti open-redirect), UI auth (`(auth)/*` + `components/auth/*` + fix Suspense `/login`) y guard `/api/exports` (anti-escalamiento). Post-merge PASS: typecheck/lint 0, **452 tests**, build (Proxy + rutas auth + `/api/exports`), gm 22/22 + 9/9, validador 214/0/0, **RLS runtime 47/47** (14 migr + 4 seeds), **smoke demo 6/6 HTTP 200**. Sin secretos/.env.local/privados. Tag `wave-4a-auth-local-v1`. **Vercel permanece `APP_AUTH_MODE=demo` + `READ_MODEL_SOURCE=fixture`**; Supabase remoto NO conectado. B-004 (Realtime) deuda no bloqueante. Conexión remota = microfase posterior (4A.3). **Oleada 4A CERRADA; 4B NO iniciada** | ✅ Aprobado |
| 2026-06-02 | **Oleada 4A.3 — vínculo controlado Supabase remoto (read-only)** sobre proyecto `construction-ops-prod` | Login CLI (2.102.0) + `supabase link --project-ref jabddbccmhrxztfzpdii` ejecutados por la usuaria en su terminal (TTY; el entorno de herramienta es no-TTY y rechaza el login automático). `Finished supabase link.` Auditoría read-only: **remoto vacío** (`migration list --linked` = 14 locales con Remote en blanco); `supabase/.temp/` ignorado (`.gitignore:24`); sin cambios remotos. Detectado **mismatch**: remoto Postgres **17.6.1** vs `config.toml` `major_version = 15`. Sin `db push`/`pull`/`repair`/SQL/seeds/usuarios; Vercel intacto | ✅ Aprobado |
| 2026-06-02 | **Oleada 4A.3a — paridad local Postgres 17 + `db push --dry-run`** sobre rama `integration/wave-4a3-remote-bootstrap` (NO merge a main) | `supabase/config.toml` `major_version` **15 → 17** (cambio local, reversible, orchestrator-owned) para paridad con el remoto PG 17.6.1; mejora la validación de shadow-DB y `db diff`. Revalidación local en PG17 + `db push --dry-run --linked` (no aplica nada). **Sin push real al remoto en esta sesión**; sin `--include-seed`; sin seeds/SQL/usuarios remotos; Vercel intacto (`APP_AUTH_MODE=demo` + `READ_MODEL_SOURCE=fixture`) | ✅ Aprobado |
| 2026-06-02 | **Oleada 4A.3b — merge paridad PG17 a `main` + bootstrap real del esquema remoto** | `git merge --no-ff integration/wave-4a3-remote-bootstrap` → `139dd52` (sin conflictos); tag `wave-4a3-pg17-bootstrap-ready-v1`. `supabase db push --linked` **una sola vez** (sin `--include-seed`) ⇒ **14/14 migraciones Local = Remote** en `construction-ops-prod`. Post-merge PASS (typecheck/lint 0, 452 tests, build, gm 22/22 + import, validador 214/0/0). **Esquema remoto presente, SIN datos funcionales** (sin org/usuarios reales). Seeds NO ejecutados; usuarios NO creados; sin SQL/`migration repair`; **Vercel intacto** (`demo`+`fixture`). Tag `wave-4a3-remote-schema-bootstrap-v1`. Rollback: mantener `demo`+`fixture`; ante fallo futuro pre-datos, corregir o recrear proyecto vacío (NO `migration repair` automático) | ✅ Aprobado |
| 2026-06-02 | **Oleada 4A.3c (fix) — cablear login online a Supabase** sobre rama `fix/wave4a3-online-login` (NO merge a main) | Causa raíz: el cliente de navegador resolvía las variables `NEXT_PUBLIC_*` de forma **indirecta** (`env.X` con `env=process.env`); Next solo inyecta en el bundle las referencias **literales** `process.env.NEXT_PUBLIC_*`, así que en el navegador quedaban `undefined` y `getPublicSupabaseEnv()` lanzaba **antes** de llamar a `signInWithPassword()` (de ahí el error genérico sin request a `/auth/v1/token` ni log de Auth). Fix: `client.ts` pasa referencias **literales** inyectables; lógica de submit extraída a `server/auth/login-flow.ts` (`runPasswordLogin`, pura/testeable: 1 sola llamada a Supabase, error legible, redirección `next` sanitizada). Regla: **nunca** llamar `getPublicSupabaseEnv()` sin args desde código de navegador. Sin tocar Vercel/DB remota; typecheck/lint 0, **461 tests**, build, gm 22/22, validador 214/0/0 | ✅ Aprobado |
| 2026-06-02 | **Oleada 4A.3c — merge del fix de login online a `main`** | Aprobado por la usuaria. `git merge --no-ff fix/wave4a3-online-login` (`834029c`) → merge **`ad8f32b`**, **sin conflictos** (8 archivos, +272/-12). Post-merge PASS: typecheck/lint 0, **461 tests**, build (rutas auth + Proxy + `/api/exports`), gm 22/22, gm:import, validador 214/0/0, `git diff --check` limpio. **DB remota intacta** (14/14 migraciones; sin push/pull/repair/SQL/seeds/usuarios); **Vercel intacto** (`demo`+`fixture`). Tag `wave-4a3-online-login-fix-v1`. Activación online = paso manual posterior (`APP_AUTH_MODE=supabase` + `fixture` + redeploy); rollback = `demo`+`fixture`. **4B NO iniciada** | ✅ Aprobado |
| 2026-06-02 | **CIERRE Oleada 4A.3 — Supabase remoto + autenticación online validada** | La usuaria activó manualmente `APP_AUTH_MODE=supabase` (+ `READ_MODEL_SOURCE=fixture`) en producción y confirmó el smoke real: `/login` visible → login del admin (`GRUPO ICONIC`, rol `admin`) → sesión creada → redirect `/dashboard` → dashboard fixture sanitizado visible (`/logout` = comprobación final manual pendiente). Auditoría read-only `migration list --linked` ⇒ **14/14 Local = Remote** (PG17); seeds demo remotos 0. Sin secret/service-role en frontend; sin datos reales de presupuesto expuestos; **`READ_MODEL_SOURCE=db` NO activado**. **Vercel a mantener**: `APP_AUTH_MODE=supabase` + `READ_MODEL_SOURCE=fixture`; rollback = `demo`+`fixture`+redeploy. Tag `wave-4a3-online-auth-validated-v1`. **Oleada 4A.3 CERRADA; 4B propuesta pero NO iniciada** | ✅ Aprobado |
| 2026-06-02 | **Oleada 4B.1 — contrato congelado del vertical slice real de proyectos** (`docs/PROJECTS_CRUD_CONTRACT.md` v1) sobre rama `integration/wave-4b1-real-projects` (NO merge a main) | Primer flujo de **escritura** real (crear/listar/detalle) contra Supabase DB con RLS. Decisiones: (1) **nueva capa de escritura** `ProjectsWriteRepository` (`apps/web/server/projects/`), el `ReadModelPort` sigue solo-lectura; (2) migración nueva agrega `projects.created_by` (FK `profiles`, ON DELETE SET NULL) y `projects.description`; (3) **"Ciudad" persiste en `location`** (sin columna nueva); (4) `code` **autogenerado server-side** (slug + anti-colisión) por ser NOT NULL UNIQUE(org,code); (5) `organization_id`/`created_by`/`code`/`id`/timestamps SIEMPRE server-side, nunca del navegador; (6) creación exige `APP_AUTH_MODE=supabase` + `READ_MODEL_SOURCE=db` (en demo/fixture deshabilitada); (7) cliente RLS-bound, **sin service-role**; cross-org ⇒ 404 amable. Ejecución secuencial db-rls→frontend-boq, validación local; **sin tocar Vercel ni remoto productivo** | ✅ Aprobado |
| 2026-06-02 | **Oleada 4B.1 — vertical slice real de proyectos IMPLEMENTADO y validado** (rama `integration/wave-4b1-real-projects` `2397323`; NO merge a main) | Fase 2 (db-rls, backup `3a403bd`) + Fase 3 (frontend-boq, backup `6f41fd4`) integradas con `--no-ff`. Migración `created_by`+`description`; `ProjectsWriteRepository` (createProject/getProjectById, RLS-bound sin service-role, `code` autogenerado); server action con viewer real server-side + `mode-guard` (creación solo `supabase`+`db`); lista/formulario/detalle. Validado: typecheck/lint 0, **505 tests**, build, gm 22/22, gm:import, validador 214/0/0, **RLS runtime 58/58** (PG17), smoke HTTP demo+fixture (rutas 200, "+ Nuevo proyecto" deshabilitado en demo). Pendiente no bloqueante: smoke interactivo navegador en modo `db`. **Sin tocar Vercel/remoto; NO merge a main; 4C NO iniciada** | ✅ Aprobado |
| 2026-06-02 | **Oleada 4B.1 — merge a `main` + migración remota de proyectos** | Aprobado por la usuaria. `git merge --no-ff integration/wave-4b1-real-projects` (`1f5f908`) → merge **`10ac567`**, sin conflictos (27 archivos, +2549/-26). Post-merge PASS: typecheck/lint 0, **505 tests**, build, gm 22/22, gm:import, validador 214/0/0. `db push --dry-run` ⇒ 1 migración pendiente + 0 seeds; `db push --linked` (sin `--include-seed`) aplicó `20260602120000_projects_authorship.sql` ⇒ **15/15 Local = Remote**. **Seeds NO ejecutados; proyectos remotos NO creados; sin SQL/`migration repair`/service-role; Vercel intacto** (`supabase`+`fixture`). Tags `wave-4b1-projects-code-ready-v1` y `wave-4b1-projects-remote-ready-v1`. Próximo paso manual de la usuaria: `READ_MODEL_SOURCE` `fixture`→`db` + redeploy + crear primer proyecto (smoke remoto); rollback = `fixture`+redeploy. **4B.2/4C NO iniciadas** | ✅ Aprobado |
| 2026-06-02 | **Fix 4B.1 — membresía en modo `db`: grants de `app` + self-RLS en `profiles`** (rama `fix/wave4b1-membership-resolution`; NO merge a main) | Causa raíz doble demostrada local: (#1) las migraciones no concedían a `authenticated` `USAGE`/`EXECUTE` del esquema `app` (el harness lo daba en runtime, enmascarando) ⇒ remoto "permission denied for schema app" reportado como "no membresía"; (#2) `profiles_select` (`organization_id = current_org()`) recursa cuando el `SECURITY DEFINER` no salta RLS (postgres remoto sin `BYPASSRLS`) ⇒ "stack depth limit exceeded". Fix (migración `20260602130000`): `GRANT USAGE/EXECUTE` de `app` a `authenticated` + reemplazo por `profiles_self_select USING (id = (SELECT app._auth_uid()))` (self-read sin `current_org()`, **más estricto**, aislamiento intacto, RLS sigue FORCE). Harness deja de enmascarar grants de `app` y +3 checks. Validado: **RLS runtime 61/61**, typecheck/lint 0, **505 tests**, build, gm 22/22, validador 214/0/0. Sin escritura remota; Vercel intacto. **Hotfix autorado por el orquestador (a avalar por agent-db-rls).** | ✅ Aprobado |
| 2026-06-02 | **Fix 4B.1 — merge a `main` + migración remota correctiva** | Aprobado por la usuaria. `git merge --no-ff fix/wave4b1-membership-resolution` (`ab08b28`) → merge **`82c2fa7`**, sin conflictos (7 archivos, +186/-5). Post-merge PASS: typecheck/lint 0, **505 tests**, build, gm 22/22, gm:import, validador 214/0/0. `db push --dry-run` ⇒ 1 migración pendiente + 0 seeds; `db push --linked` (sin `--include-seed`) aplicó `20260602130000_fix_membership_app_grants_and_profiles_self_rls.sql` ⇒ **16/16 Local = Remote**. **Seeds NO ejecutados; proyectos remotos NO creados; sin SQL/`migration repair`/service-role; Vercel intacto** (`supabase`+`fixture`). Tags `wave-4b1-membership-fix-code-ready-v1` y `wave-4b1-membership-fix-remote-ready-v1`. Próximo paso manual de la usuaria: `READ_MODEL_SOURCE` `fixture`→`db` + redeploy + repetir smoke; rollback = `fixture`+redeploy. **4B.2/4C NO iniciadas** | ✅ Aprobado |
| 2026-06-03 | **Fix 4B.1 — `/projects/new` request-time (mode-guard)** (rama `fix/wave4b1-project-creation-mode-guard`; NO merge a main) | Causa raíz: `/projects/new` se prerenderizaba **estáticamente** (no usa `cookies()`/`headers()`), evaluando `isCreationModeEnabled()` en build-time con defaults `demo`+`fixture` ⇒ bloqueaba creación aunque en runtime `READ_MODEL_SOURCE=db`. Fix mínimo: `export const dynamic = 'force-dynamic'` en la página ⇒ render request-time (build: `○`→`ƒ /projects/new`). La server action ya guardaba en request-time (sin cambios). **Convención**: páginas cuyo render dependa de env de servidor de runtime (`APP_AUTH_MODE`/`READ_MODEL_SOURCE`) deben ser `force-dynamic` o usar una API dinámica. Validado: typecheck/lint 0, **507 tests**, build, gm 22/22, validador 214/0/0. Solo código (sin migración); sin tocar Vercel/remoto | ✅ Aprobado |
| 2026-06-03 | **Fix `/projects/new` — merge a `main`** | Aprobado por la usuaria. `git merge --no-ff fix/wave4b1-project-creation-mode-guard` (`9e9dd96`) → merge **`1999ffb`**, sin conflictos (5 archivos, +102). Post-merge PASS: typecheck/lint 0, **507 tests**, build con **`ƒ /projects/new`**, gm 22/22, gm:import, validador 214/0/0, `git diff --check` limpio. **Solo código** (sin migración): remoto intacto **16/16**, seeds 0, proyectos 0; **Vercel intacto** (`supabase`+`fixture`). Tag `wave-4b1-project-creation-route-fix-v1`. Próximo paso manual de la usuaria: `READ_MODEL_SOURCE` `fixture`→`db` + redeploy + smoke de creación; rollback = `fixture`+redeploy. **4B.2/4C NO iniciadas** | ✅ Aprobado |
| 2026-06-03 | **Fix 4B.1 residual — `/projects/new` intrínsecamente dinámica** (rama `fix/wave4b1-runtime-creation-env`; NO merge a main) | Auditoría profunda demostró que el **código YA era correcto**: sin literales `process.env.*` (no inlining); chunk compilado lee `a.READ_MODEL_SOURCE`/`a.APP_AUTH_MODE` con `a=process.env` (runtime); `/projects/new` es `ƒ` sin prerender ni entrada en `prerender-manifest` (force-dynamic efectivo); el Proxy local leyó `supabase` en runtime. **Causa residual = caché estática/edge OBSOLETA de despliegues previos (`○`), ambiental, no de código.** Endurecimiento: `/projects/new` ahora `async` + `await resolveViewer()` (señal dinámica **intrínseca** vía `cookies()`, como `/projects`) + se conserva `force-dynamic`; así Vercel no la sirve desde caché estática y el deploy supera el HTML obsoleto; además defensa en profundidad (no solo Proxy). **Convención reforzada**: rutas cuyo render dependa de env de servidor de runtime deben usar señal dinámica intrínseca (cookies/headers), no solo el flag. Validado: typecheck/lint 0, **508 tests**, build `ƒ /projects/new`, gm 22/22, validador 214/0/0. Solo código (sin migración). **Recomendación de deploy: build limpio sin caché + purga de caché del proyecto** | ✅ Aprobado |
| 2026-06-04 | **Hardening `/projects/new` — merge a `main`** | Aprobado por la usuaria. `git merge --no-ff fix/wave4b1-runtime-creation-env` (`7b112cc`) → merge **`0ad7f56`**, sin conflictos (5 archivos, +102/-8). Post-merge PASS: `/projects/new` conserva `force-dynamic` + `await resolveViewer()` (dinámica intrínseca); typecheck/lint 0, **508 tests**, build con **`ƒ /projects/new`** (sin prerender; ausente del prerender-manifest), gm 22/22, gm:import, validador 214/0/0, `git diff --check` limpio. **Solo código** (sin migración): remoto intacto **16/16**, seeds 0, proyectos 0; **Vercel intacto** (`supabase`+`fixture`). Tag `wave-4b1-project-creation-runtime-hardening-v1`. Hipótesis ambiental (caché obsoleta) aún no confirmada. Próximo paso manual de la usuaria: `READ_MODEL_SOURCE` `fixture`→`db` + **redeploy SIN Build Cache** + smoke `/projects/new`; rollback = `fixture`+redeploy. **4B.2/4C NO iniciadas** | ✅ Aprobado |
| 2026-06-04 | **Fix 4B.1 — `/dashboard` maneja DB vacía sin proyecto demo** (rama `fix/wave4b1-empty-db-dashboard-build`; NO merge a main) | Causa raíz: `/dashboard` era **estática** (`○`, usaba `getDemoViewer()`) y **hardcodeaba** `DEMO_PROJECT_ID='…010'`, invocando `getDashboardSummary(viewer, DEMO_PROJECT_ID)` sin try/catch. En modo `db` con base productiva vacía, el prerender de build ejecutaba la página ⇒ `projectById`→null ⇒ `ProjectNotFoundError` ⇒ "Error occurred prerendering page /dashboard". Único causante; las hermanas ya manejaban vacío. Fix mínimo: `export const dynamic='force-dynamic'` + `await resolveViewer()` (viewer real por modo, dinámica intrínseca vía cookies como `/projects/new`), proyecto activo derivado de `listProjects` + helper puro `selectActiveProjectId` (sin UUID demo), **empty state** con CTA a `/projects/new` (gated `isCreationModeEnabled`), `getDashboardSummary` en try/catch, aviso "Modo fixture" condicionado a `READ_MODEL_SOURCE=fixture`. Sin fallback silencioso db→fixture. **Convención reforzada**: páginas que resuelvan un proyecto activo NO deben asumir un proyecto demo; deben derivarlo de datos reales y manejar org vacía. Validado: typecheck/lint 0, **520 tests** (+12), build fixture `ƒ /dashboard` (antes `○`), **build modo `db` LOCAL PASS** en DB sembrada y **DB vacía** (proyectos truncados localmente: 2 orgs/10 profiles/0 proyectos), gm 22/22, gm:import, validador 214/0/0. **Solo código** (sin migración): **remoto NO tocado** (trabajo contra Postgres local), **16/16**, seeds 0, proyectos 0; **Vercel intacto** (`supabase`+`fixture`). **4B.2/4C NO iniciadas** | ✅ Aprobado |
| 2026-06-04 | **Fix `/dashboard` DB vacía — merge a `main`** | Aprobado por la usuaria. `git merge --no-ff fix/wave4b1-empty-db-dashboard-build` (`d1aa929`) → merge **`b942f3f`**, sin conflictos (6 archivos, +331/-25). Post-merge PASS: typecheck/lint 0, **520 tests**, build con **`ƒ /dashboard`** y **`ƒ /projects/new`**, gm:regression **22/22**, gm:import PASS, validador **214/0/0**, `git diff --check` limpio. **Solo código** (sin migración): remoto intacto **16/16**, seeds 0, proyectos 0; **Vercel intacto** (`supabase`+`fixture`; `DATABASE_URL` configurada privadamente por la usuaria, no expuesta). Tag `wave-4b1-empty-db-dashboard-ready-v1`. **Deuda registrada**: rutas hermanas estáticas `/apu`,`/catalog`,`/quantities`,`/planning` deben migrarse a viewer real request-time antes de uso productivo multitenant (ver INTEGRATION_REQUESTS). Próximo paso manual de la usuaria: `READ_MODEL_SOURCE` `fixture`→`db` + redeploy SIN Build Cache + smoke de creación; rollback = `fixture`+redeploy. **4B.2/4C NO iniciadas** | ✅ Aprobado |
| 2026-06-04 | **Estabilización de producción 4B.1 — db mode + deploy** (rama `fix/wave4b1-production-stabilization` → merge `c6d0ad1`) | Diagnóstico integral del estado mixto en producción: (1) CTAs `+ Nuevo proyecto`/`Crear primer proyecto` no navegaban por patrón `<Link><Button></Button></Link>` (HTML `<a><button></button></a>`, interactivo anidado inválido); (2) `/apu`,`/catalog`,`/quantities`,`/planning` estáticas y `/estimates` con `getDemoViewer()` exponían demo/fixture en `db`; (3) footer hardcodeado `Oleada 3A — fixture`. **Fix**: CTAs → `<Button asChild><Link href="/projects/new">` (ancla navegable); rutas hermanas → `force-dynamic` + `resolveViewer()` (viewer real en db, demo en fixture), `/planning` sin UUID demo, empty state honesto; layout `force-dynamic`; footer → `readModelModeLabel()` (db="Datos reales", fixture="Modo demostración"). **Convención**: todo el segmento autenticado `(dashboard)` es request-time vía `force-dynamic` en su layout. Validado: typecheck/lint 0, **546 tests** (+26), build fixture + build `db` LOCAL (vacío y sembrado) PASS (rutas dashboard `ƒ`), gm 22/22, gm:import, validador 214/0/0. **Preview Vercel READY** (build production-like; Preview tras SSO del equipo). **Production READY** aliased a `construction-ops-psi.vercel.app`; smoke: `/login` 200, rutas protegidas 307→/login (sin fixture público, sin 500). **Sin escrituras remotas; variables Vercel NO modificadas (solo nombres listados); pruebas db contra Postgres local**. `.gitignore`: +`.vercel`, −`.env*` redundante. Única acción manual de la usuaria: login + crear primer proyecto. Rollback = `READ_MODEL_SOURCE=fixture` + redeploy. **4B.2/4C NO iniciadas** | ✅ Aprobado |

| 2026-06-04 | **Oleada 4B.1 CERRADA — smoke productivo real exitoso** | La usuaria validó en producción (`construction-ops-psi.vercel.app`, `supabase`+`db`): login real, footer "Datos reales", `/projects` + CTA + formulario funcionales, **proyecto remoto real creado desde la UI** (`ENTRE PATIOS`, Cali, Activo), redirect y detalle `/projects/[id]` OK, **sin fixture en DB mode**. create/list/detail verificados end-to-end. `main = b782502`. Tag `wave-4b1-real-projects-production-v1`. Rollback estable = ese tag. **Siguiente: Oleada 4B.2 (alcances reales por proyecto).** | ✅ Aprobado |

| 2026-06-04 | **Oleada 4B.2 — alcances reales (scopes) por proyecto** (rama `integration/wave-4b2-real-scopes`) | Migración aditiva autorizada `20260604120000_project_scopes_authorship` (`project_scopes` += `description`, `created_by` FK profiles ON DELETE SET NULL + índice). Dry-run = 1 migración esperada; `db push --linked` (sin seeds) ⇒ **17/17 Local = Remote**. RLS sin cambios (suficiente). Capa `apps/web/server/scopes/` (`insertScope`/`listScopesByProject`/`getScopeById`, RLS-bound sin service-role, code autogenerado, `created_by`/`status`/`project_id` server-side, proyecto validado por visibilidad). Constantes client-safe en `@/lib/scopes/scope-types` (evita `postgres` en el bundle). `scope_type` = **select** (7 valores, default `floor`); `created_by` derivado del viewer. UI: sección Alcances en `/projects/[id]` + `/scopes/new` + `/scopes/[scopeId]`, request-time, CTAs `Button asChild`+`Link`, empty state honesto, placeholder de presupuesto (4B.3). Validado: typecheck/lint 0, **588 tests** (+42), build fixture + db local (vacío/sembrado), gm 22/22, validador 214/0/0, **RLS runtime 67/67**. Contrato congelado `docs/SCOPES_CRUD_CONTRACT.md`. **4B.3/4C NO iniciadas.** | ✅ Aprobado |

| 2026-06-04 | **Oleada 4B.2 CERRADA — smoke productivo real de alcances** | La usuaria validó en producción (`construction-ops-psi.vercel.app`, `supabase`+`db`): **alcance remoto real creado desde la UI** (`PRIMER PISO`, tipo floor, con descripción) en el proyecto `ENTRE PATIOS`; listado, detalle y placeholder de presupuesto funcionales; footer "Datos reales"; **sin fixture en DB mode**. create/list/detail verificados end-to-end. `main = 0f470bf`. Tag `wave-4b2-real-scopes-production-v1`. Rollback estable = ese tag. **Siguiente: Oleada 4B.3 (presupuesto inicial real por alcance, estimate + V01).** | ✅ Aprobado |

| 2026-06-04 | **Oleada 4B.3 — presupuesto inicial real + V01 por alcance** (rama `integration/wave-4b3-real-estimates`) | Migración aditiva autorizada `20260604130000_estimates_authorship_and_atomic_create` (`estimates` += `description`, `created_by`; RPC `create_estimate_with_initial_version`). **Ajuste de seguridad obligatorio aplicado**: la RPC NO recibe `p_created_by`; deriva el autor de `app._auth_uid()` (helper canónico) ⇒ imposible suplantar. `SECURITY INVOKER` (RLS aplica), estimate `active` + V01 `draft` atómico; hardening `search_path`/refs calificadas/`REVOKE FROM PUBLIC+anon`/`GRANT TO authenticated`. Dry-run = 1 migración; `db push` ⇒ **18/18**. Capa `apps/web/server/estimates/` (selector sin fallback, db RLS-bound vía RPC, fixture solo lectura). "Versión activa" = mayor `version_number`. UI: sección Presupuestos en el alcance + `/estimates/new` + `/estimates/[estimateId]` (V01, 0/0, placeholder Excel) + `/estimates` reintegrada como listado real. Validado: typecheck/lint 0, **623 tests** (+35), build fixture + db local (vacío/sembrado), gm 22/22, validador 214/0/0, **RLS runtime 77/77**. Contrato `docs/ESTIMATES_CRUD_CONTRACT.md`. **4C NO iniciada.** | ✅ Aprobado |
| 2026-06-04 | **Oleada 4B.3 CERRADA — smoke productivo real de presupuesto + V01** | La usuaria validó en producción (`supabase`+`db`): **presupuesto remoto real creado desde la UI** (`PRESUPUESTO BASE`) con **V01** activa en ENTRE PATIOS → PRIMER PISO; conteos 0 capítulos / 0 ítems; placeholder de importación visible; **sin fixture en DB mode**. `main = bef4786`. Tag `wave-4b3-real-estimates-production-v1`. Rollback estable = ese tag. **Siguiente: Oleada 4C.1 (importación controlada de Excel a V01: preview → confirmación transaccional).** | ✅ Aprobado |

| 2026-06-05 | **Oleada 4C.1 — importación controlada de Excel a V01** (rama `integration/wave-4c1-excel-import`) | Migración aditiva autorizada `20260604140000_boq_import_atomic` (RPC `import_boq_into_version`, sin tablas nuevas ni cambios RLS). RPC `SECURITY INVOKER`: deny sin sesión/membresía, `FOR UPDATE` + versión editable+vacía (anti doble-submit), inserta capítulos+ítems atómicos con **subtotal recalculado server-side**; grants `REVOKE PUBLIC+anon`/`GRANT authenticated`. Dry-run = 1 migración; `db push` ⇒ **19/19**. Formato congelado v1: `.xlsx`, hoja `COTIZACION 1 PISO`, columnas por encabezado, convención capítulo/ítem. Flujo **dos pasos sin persistir archivo**: preview (parse + digest SHA-256, sin escritura) → confirm (re-parse + compara digest + RPC). `subtotal = quantity×unit_price` (Decimal), nunca confiado del cliente/Excel. Límites: archivo **3 MB** (`serverActions.bodySizeLimit='4mb'` en next.config, < ~4.5 MB Vercel), 500 cap/5000 ítems, negativos bloqueados. SheetJS no evalúa fórmulas/macros. Validado: typecheck/lint 0, **651 tests** (+28), build fixture + db local (vacío/sembrado), gm 22/22, validador 214/0/0, **RLS runtime 87/87**. Contrato `docs/EXCEL_IMPORT_CONTRACT.md`. Excel privado real NO usado (fixtures sintéticos). **4C.2/4D NO iniciadas.** | ✅ Aprobado |
| 2026-06-05 | **Oleada 4C.2 — compatibilidad con plantilla real de cotización** (rama `integration/wave-4c1-excel-import`, fix de parser) | La 1.ª prueba con el Excel real falló en preview (`Fila 22: capítulo sin código`). Causa: `SUBTOTAL CAPITULO` clasificado como capítulo + fila desfasada por `blankrows:false`. Fix **solo de parser (SIN migración)**: `blankrows:true` (fila real); palabras reservadas (`SUBTOTAL CAPITULO` ignorado; `TOTAL COSTOS DIRECTOS` cierra el BOQ; AIU/control de pagos ignorados); plantilla de 7 columnas (`CAP` auxiliar ignorada, `ÍTEM`=code, `VR. PARCIAL`=subtotal de comparación); **diagnóstico agregado** (recorre toda la hoja; `errors[]`+`warnings[]` con fila real; confirmación bloqueada si hay errores vía `ImportHasErrorsError`). **Duplicados sin normalización silenciosa** (Opción A): capítulo duplicado=ERROR (constraint único por versión), ítem duplicado=ADVERTENCIA (BD no lo restringe); no se añade `source_code`. Validado: typecheck/lint 0, **651 tests**, build fixture + db local, gm 22/22, validador 214/0/0. Sin migración ⇒ remoto intacto **19/19**; no se confirmó importación (V01 vacía). Excel privado real NO usado. **4D NO iniciada.** | ✅ Aprobado |
| 2026-06-05 | **Oleada 4C.3 — normalización controlada y reversible de códigos** (rama `integration/wave-4c3-source-normalization`) | Tras validar 4C.2 con el Excel real (10 capítulos aceptados, duplicados 7/8/9/10), se autoriza la migración aditiva `20260605120000_boq_source_traceability`: `chapters`+`boq_items` += `source_code text` + `source_row integer` (+ CHECK `source_row>0`); RPC `import_boq_into_version` (MISMA firma) persiste `code`=canónico + `source_code` + `source_row`. Dry-run = 1 migración; `db push` ⇒ **20/20**. Parser conserva `sourceCode`/`sourceRow` y propone `canonicalCode`: capítulos duplicados ⇒ siguiente entero por encima del máximo (genérico: 7→11/8→12/9→13/10→14); ítems ⇒ propaga prefijo del capítulo canónico (7.01→11.01; histórico 2.0x bajo cap3→3.0x); ambiguo ⇒ `requiresManualReview` (bloquea). Mapping por clave **`rowType:sourceRow`** (no por `sourceCode`). El navegador solo envía intención (`overrides`); reconstrucción y validación server-side; digest del preview = payload **ORIGINAL** (integridad, estable ante overrides). UI "Revisar numeración" (tabla editable + Revalidar). Validado: typecheck/lint 0, **650 tests**, build fixture + db local, gm 22/22, validador 214/0/0, **RLS runtime 89/89**. Excel privado real NO usado. **4D NO iniciada.** | ✅ Aprobado |
| 2026-06-05 | **Oleada 4C.3 CERRADA — smoke real de importación con normalización** | La usuaria validó en producción (`supabase`+`db`): subió el **Excel histórico ORIGINAL** a PRESUPUESTO BASE → V01; upload → preview → sugerencias visibles → normalización controlada → Confirmar → "Importación completada". `source_code`/`source_row` aplicados al archivo histórico; **V01 ya contiene datos reales**. No usó la copia corregida. `main = 4071be8`. Tag `wave-4c3-real-excel-import-production-v1`. Rollback estable = ese tag. **Siguiente: Oleada 4D.1 (revisión operativa del presupuesto importado).** | ✅ Aprobado |

| 2026-06-05 | **Oleada 4D.1 — revisión operativa del presupuesto importado** (rama `integration/wave-4d1-budget-review`) | **Sin migración** (esquema + `source_code`/`source_row` de 4C.3 bastan). Métodos de lectura RLS-bound añadidos al `EstimatesWriteRepository` (db+fixture): `listChaptersByEstimateVersion`/`getChapterById`/`listItemsByChapter` (resumen reusa `getEstimateById`). UI: detalle del presupuesto con resumen (V01/capítulos/ítems/total directo) + tabla de Capítulos (indicador discreto "normalizado" si `source_code≠code`) + nueva ruta `…/chapters/[chapterId]` con contexto + tabla de Ítems BOQ + trazabilidad secundaria. Cross-org bloqueado; reimport bloqueada. Tipos client-safe `@/lib/estimates/review-types`. Validado: typecheck/lint 0, **666 tests** (+16), build fixture + db local, gm 22/22, validador 214/0/0. Remoto intacto **20/20**; sin cambios remotos. **4D.2/4E NO iniciadas.** | ✅ Aprobado |
| 2026-06-05 | **Oleada 4D.1 CERRADA — smoke real de revisión operativa** | La usuaria validó en producción: PRESUPUESTO BASE → V01 con 14 capítulos, ~132 ítems, total directo visible; capítulos navegables; ítems BOQ visibles; códigos canónicos + trazabilidad histórica correctos; etiquetas "normalizado" donde corresponde. `main = e3ad4af`. Tag `wave-4d1-operational-budget-review-production-v1`. Rollback estable = ese tag. **Siguiente: Oleada 4D.2 (AIU editable + costos indirectos + total general por versión).** | ✅ Aprobado |

| 2026-06-05 | **Oleada 4D.2 — AIU editable + costos indirectos + total general por versión** (rama `integration/wave-4d2-editable-aiu`) | **Sin migración**: reutiliza `indirect_cost_rules` (porcentajes por `estimate_version`, RLS completa, seed A/I/U/IVA). **Decisión**: AIU **por versión, editable, NO global/hardcodeado**; no se precargan `3.5/2.5/4/19` como default. Cálculo puro `aiu-calc.ts` (Decimal): humano `3.5`→fracción `0.035`, rango [0,100]; fórmulas A/I/U sobre directTotal, IVA sobre utilidad, indirectTotal, grandTotal. Repo (db RLS-bound + fixture): `getEstimateVersionAiu`/`updateEstimateVersionAiu` (upsert atómico PostgREST onConflict; fixture solo lectura)/`calculateEstimateFinancialSummary`; `directTotal`=Σ BOQ server-side. UI: sección "AIU y costos indirectos" con preview client-side + cálculo definitivo server-side + total general; versión emitida read-only. El navegador solo envía los 4 %. Validado: typecheck/lint 0, **687 tests** (+21), build fixture + db local, gm 22/22, validador 214/0/0, **RLS runtime 93/93**. Contrato `docs/AIU_CRUD_CONTRACT.md`. Deudas: `AIU_PRESETS_BY_ORGANIZATION`, `AIU_IMPORT_PREFILL_FROM_EXCEL`. Remoto intacto **20/20**. **4D.3/4E NO iniciadas.** | ✅ Aprobado |

Decisiones abiertas:
- [ ] Nombre final del producto
- [x] **Q9 — Política de redondeo decimal COP** — ✅ RESUELTA 2026-05-30 (ver tabla)
- [x] **Q8 — Base exacta del descuento** — ✅ RESUELTA 2026-05-30 (`online_public_price`; ver tabla)
- [ ] Usuarios iniciales y roles asignados
- [ ] Qué información ve el cliente en APU
- [ ] Proveedores visibles para cliente
- [ ] Frecuencia de sincronización de precios
- [x] **Q14 — Canal oficial Homecenter** — ✅ RESUELTA 2026-05-30 (MVP CSV/Excel + adaptador genérico; ver tabla)
- [x] **Q11 — Aprobación humana (firma simple vs doble)** — ✅ RESUELTA 2026-05-30 (simple en MVP; doble futura configurable; ver tabla)
- [ ] Gantt por capítulos o actividades
- [ ] Ubicación de despliegue (Vercel + Railway o solo Vercel)
