# Solicitudes de integración entre agentes

Formato:
Agente solicitante | Archivo o contrato que necesita | Agente responsable | Estado

## Solicitudes

| Solicitante | Archivo / necesidad | Responsable | Estado |
| agent-orchestrator (QUANTITY_WORKSPACE_AND_BOQ_SYNC_V1) | **`QUANTITY_WORKSPACE_RELEASE_REQUIREMENTS`** — Antes del release: `supabase db push` de `20260620090000` + `20260620090100` con gate dry-run estricto (2 migraciones aditivas esperadas: tablas `quantity_workspace_groups/lines`, triggers same-org/updated_at, RPC `update_boq_item_quantity`, CHECK `apu_manual_actions` += `update_quantity`, RLS). Tras el release: crear un grupo de cantidades (incl. plantilla muro mixto) desde `/quantities/workspace/new`, enviarlo al presupuesto con preview (rol management/internal), verificar catálogo con estados de precio y descargar PDF cliente vs técnico. | agent-orchestrator / agent-db-rls | ⏳ PENDIENTE — requirements de release |
| agent-orchestrator (QUANTITY_WORKSPACE_AND_BOQ_SYNC_V1) | **`QUANTITY_WORKSPACE_RLS_HARNESS_V1`** — Añadir sección dedicada en `scripts/rls-runtime/run.ts` para `quantity_workspace_groups/lines` (aislamiento cross-org, roles de escritura, `update_boq_item_quantity` bloquea versión emitida y preserva `unit_price_snapshot`). Hoy cubierto por preflight FORCE=38 + paridad de guards con `add_apu_to_boq`. | agent-qa / agent-db-rls | ⏳ PENDIENTE — endurecimiento de QA |
| agent-orchestrator (APU_MANUAL_BUILDER_V1) | **`APU_MANUAL_BUILDER_RELEASE_REQUIREMENTS`** — Antes del release: `supabase db push` de `20260618090000` con gate dry-run estricto (1 migración aditiva esperada: `apu_templates` +origin_type/created_by, tabla `apu_manual_actions`, RPCs `create_manual_apu`/`add_apu_to_boq`). Tras el release: crear un APU manual desde `/apu/new` y agregar una actividad al BOQ de una versión editable con revisión visual (rol management/internal). | agent-orchestrator / agent-db-rls | ⏳ PENDIENTE — requirements de release |
| agent-orchestrator (APU_MANUAL_BUILDER_V1) | **`APU_EXPORTS_V1`** — Exportación de un APU (PDF/Excel) con desglose de componentes, perfiles de privacidad por rol. | agent-exports | ✅ RESUELTO (2026-06-13, `feature/apu-budget-exports-v1`) — APU vinculados Excel (ÍNDICE APU + hoja por APU) y PDF (portada + índice + fichas) con desglose por tipo, trazabilidad y BOQ vinculado; privacidad backend-first (`getApuDetail` omite rol salarial para `client`); sanitización formula-injection; SOLO APU vinculados (no la biblioteca). |
| agent-orchestrator (APU_MANUAL_BUILDER_V1) | **`BUDGET_EXPORT_WITH_APU_ANNEX_V1`** — Desde un presupuesto seleccionado: descargar presupuesto (PDF/Excel) + APU vinculados (PDF/Excel) + paquete completo con anexos. | agent-exports | ✅ RESUELTO (2026-06-13, `feature/apu-budget-exports-v1`) — 6 documentos vía `GET /api/estimates/export?kind=budget\|apu\|package`; paquete reutiliza `addBudgetSheets`/`buildBudgetPage` sin alterar el golden master; UI menú «Exportar» con conteos/advertencias. Snapshots BOQ para valores; cálculo actual del APU para fichas. |
| agent-orchestrator (APU_EXPORTS_V1) | **`READ_MODEL_ARCHIVED_AT`** — `listApus`/`getApuDetail` deben exponer `archived_at`/`origin_type` por plantilla (el schema Drizzle no los mapeaba). | agent-db-rls | ✅ RESUELTO (2026-06-13) — mapeo ORM sincronizado (sin migración; columnas ya en BD desde `20260618`/`20260619`); `ApuSummary` += `archivedAt`/`originType`; populado en drizzle + fixture. |
| agent-orchestrator (APU_EXPORTS_V1) | **`EXPORT_QUANTITIES_ANNEX_V1`** — Incluir memorias de cantidades (`quantity_takeoff_*`) como anexo seguro del paquete (fuera de V1). | agent-exports / agent-excel-mapper | ⏳ PENDIENTE — deuda contrato §C/§13 |
| agent-orchestrator (APU_EXPORTS_V1) | **`OPERATIONAL_ACCESS_LAYER_V1` + `SMTP_CORPORATIVO_V1` + `TRUE_DB_PAGINATION_V1` + `HARDEN_APU_COMPONENTS_FOR_ALL_V1`** — Capa de acceso/roles operativa (site/client) para gobernar export y lectura; envío por correo; paginación real de BD; endurecimiento de componentes APU. | agent-orchestrator | ⏳ PENDIENTE — deudas registradas |
| agent-orchestrator (APU_MANUAL_BUILDER_V1) | **`APU_ADVANCED_EDITOR_V2`** — Edición segura de APU manuales (encabezado, agregar/quitar componente, ajustar cantidad/desperdicio, recalcular) + equipos/subcontratos explícitos en la UI. Con advertencia cuando el APU está vinculado a BOQ (snapshots no mutan). | agent-cost-domain / agent-frontend-boq | ⏳ PENDIENTE — deuda contrato §8/§9 |
| agent-orchestrator (APU_MANUAL_BUILDER_V1) | **`APU_VERSIONING_V1`** — Versionado de plantillas APU + flujo explícito para propagar cambios de precio a presupuestos (nunca silencioso sobre emitidas). | agent-cost-domain | ⏳ PENDIENTE — deuda contrato §10 |
| agent-orchestrator (ENTRE_PATIOS_APU_IMPORT_V1) | **`APU_IMPORT_RELEASE_REQUIREMENTS`** — Antes del release: `supabase db push` de `20260615090000` + `20260615090100` con gate dry-run estricto (2 migraciones aditivas esperadas; tabla `apu_import_batches` + provenance + RPC `import_apu_batch`). Tras el release: importar el workbook real desde `/apu/import` con revisión visual de la usuaria (preview: 54 actividades, 51 vinculables) y aceptación explícita de sugerencias de materiales contra el catálogo productivo. | agent-orchestrator / agent-db-rls | ⏳ PENDIENTE — requirements de release |
| agent-orchestrator (ENTRE_PATIOS_APU_IMPORT_V1) | **`BOQ_APU_RELINK_WITH_CONFIRMATION`** — Reemplazo CONFIRMADO de `apu_template_id` existente en ítems BOQ (el importador jamás reemplaza; estado `skipped_existing`). Requiere UI de confirmación explícita + auditoría. | agent-frontend-boq / agent-cost-domain | ⏳ PENDIENTE — deuda contrato §10 |
| agent-orchestrator (ENTRE_PATIOS_APU_IMPORT_V1) | **`APU_REUSABLE_CREW_TEMPLATES_V1`** — Cuadrillas reutilizables entre APU (hoy cada template congela sus filas labor por rol). | agent-cost-domain | ⏳ PENDIENTE — deuda contrato §10 |
| agent-orchestrator (APU_COST_MODEL_FOUNDATION_V1) | **`ENTRE_PATIOS_APU_IMPORT_V1`** (FASE 4B.2) — Parser estructurado de la hoja APU del Excel real: bloque salarial → `labor_roles` (sanitizado), bloques APU → `apu_templates`+`apu_components` con `labor_role_id` y `default_tool_pct` poblados; regularización de filas históricas `unit_price_source='labor_role'` sin rol. Prerequisito: verificar coordenadas TODO_VERIFY con `gm:dump`. | agent-excel-mapper / agent-cost-domain | ✅ RESUELTO (2026-06-11, FASE 4B.2) — `feature/entre-patios-apu-import-v1`: parser estructurado (54 actividades, 217 componentes, 0 deltas > 0.01 contra el workbook real), bloque salarial → roles derivados exactos (Ayudante 16.016,814 / Oficial 20.807,439), `/apu/import` con preview + confirmación idempotente vía RPC `import_apu_batch`. Filas históricas sin rol se toleran en lectura; los flujos nuevos SIEMPRE llevan `labor_role_id`. |
| agent-orchestrator (APU_COST_MODEL_FOUNDATION_V1) | **`BOQ_APU_LINKING_V1`** — Poblar `boq_items.apu_template_id` (131 ítems) por matching de código/descripción con aprobación humana; habilita `COST_TYPE_BREAKDOWN_FOUNDATION`. Solo en versiones draft/review. | agent-cost-domain / agent-frontend-boq | ✅ RESUELTO (2026-06-11, FASE 4B.2) — Linking inequívoco por descripción normalizada + unidad canónica + occurrence index, scope de versión EDITABLE seleccionada explícitamente; estados linked/unresolved/ambiguous/skipped_existing; guard `apu_template_id IS NULL` (jamás reemplaza); validado contra el fixture real: 51 linkable / 3 unresolved / 0 ambiguous de 54 actividades. Reemplazo confirmado de vínculos existentes ⇒ deuda `BOQ_APU_RELINK_WITH_CONFIRMATION`. |
| agent-orchestrator (QUANTITY_TAKEOFF_IMPORT_V1) | **`QUANTITY_TAKEOFF_RELEASE_REQUIREMENTS`** — Antes del release: `supabase db push` de `20260616090000` + `20260616090100` con gate dry-run estricto (2 migraciones aditivas esperadas; 3 tablas `quantity_*` + RPC `import_quantity_takeoff_batch`). Tras el release: importar la hoja `CANTIDADES 1 PISO` real desde `/quantities/import` con revisión visual de la usuaria (rol management/internal) y versión BOQ editable seleccionada explícitamente. | agent-orchestrator / agent-db-rls | ⏳ PENDIENTE — requirements de release |
| agent-orchestrator (QUANTITY_TAKEOFF_IMPORT_V1) | **`QUANTITY_TAKEOFF_APPLY_TO_BOQ_V1`** — Aplicar `total_calculated` del takeoff a `boq_items.quantity_snapshot` mediante flujo de REVISIÓN dedicado con confirmación explícita y auditoría (el importador V1 jamás muta BOQ). Incluye aceptación manual de sugerencias (patrón accept del APU import). | agent-cost-domain / agent-frontend-boq | ⏳ PENDIENTE — deuda contrato §14 |
| agent-orchestrator (QUANTITY_TAKEOFF_IMPORT_V1) | **`QUANTITY_TAKEOFF_MULTI_SHEET_V1`** — Hojas `CANTIDADES` (general) y `CANT COMPLETO` + importación multi-piso; sincronización takeoff ⇄ `quantity_groups` legacy. | agent-excel-mapper | ⏳ PENDIENTE — deuda contrato §14 |
| agent-orchestrator (APU_COST_MODEL_FOUNDATION_V1) | **`QUANTITY_TAKEOFF_IMPORT_V1`** (FASE 4B.3) — Cantidades y despieces (CANTIDADES 1 PISO). | agent-excel-mapper / agent-db-rls | ✅ RESUELTO (2026-06-12, FASE 4B.3) — `feature/quantity-takeoff-import-v1`: implementado con esquema aditivo NUEVO `quantity_import_batches`/`quantity_takeoff_groups`/`quantity_takeoff_lines` (decisión de contrato §7: NO se escribe en `quantity_groups`/`quantity_lines` legacy ni en `boq_items.quantity_group_id`; el vínculo vive en `quantity_takeoff_groups.boq_item_id`), parser geométrico §3, matching solo exactos §6, RPC atómica idempotente, UI `/quantities/import`. |
| agent-orchestrator (APU_COST_MODEL_FOUNDATION_V1) | **`APU_UI_ADVANCED_EDITING_V1`** — Editor manual de APU/componentes desde UI (absorbe el alcance de `APU_CREATE_FLOW`). La vista `/apu/[id]` de 4B.1 es solo lectura. | agent-frontend-boq / agent-cost-domain | ⏳ PENDIENTE — deuda contrato §2, diferida |
| agent-orchestrator (APU_COST_MODEL_FOUNDATION_V1) | **`APU_FOUNDATION_RELEASE_REQUIREMENTS`** — Antes del release: `supabase db push` de `20260613090000` + `20260613090100` con gate dry-run estricto (2 migraciones aditivas esperadas); seed 0006 es SOLO local (no se pushea). Cambio de contrato read-model: `ReadModelPort.getApuDetail` + `ApuDetail`/`ApuComponentView` (aditivo; reflejar en `READ_MODEL_CONTRACT.md` en la próxima edición del doc). | agent-orchestrator / agent-db-rls | ⏳ PENDIENTE — requirements de release |
| agent-orchestrator (PRICE_MONITORING_AGENT_V1) | **`PRICE_MONITORING_EMAIL_NOTIFICATIONS_V1`** — Notificaciones por email del monitor (cambios detectados, fuentes con error). NO se implementa SMTP en Fase 4A; la base interna son las alertas visibles (dashboard + centro de monitoreo) y los `price_monitor_results` auditables. Se resolverá JUNTO con `OPERATIONAL_ACCESS_LAYER_V1` + SMTP. | agent-orchestrator / agent-dashboard | ⏳ PENDIENTE — deuda registrada en contrato §8 |
| agent-orchestrator (PRICE_MONITORING_AGENT_V1) | **`PRICE_MONITORING_RELEASE_REQUIREMENTS`** — Antes de habilitar el cron en producción: (1) crear `CRON_SECRET` (≥32 bytes aleatorios) en Vercel → Production; (2) confirmar Root Directory del proyecto Vercel (`apps/web` asumido; si fuera la raíz, mover `apps/web/vercel.json`); (3) `supabase db push` de `20260612090000` + `20260612090100` con gate dry-run estricto; (4) verificar registro del cron en Settings → Cron Jobs tras el primer deploy. | agent-orchestrator | ⏳ PENDIENTE — requirements de release, contrato §12 |
| agent-orchestrator (PRICE_MONITORING_AGENT_V1) | **`OPERATIONAL_ACCESS_LAYER_V1`** — Capa de acceso operativo (no iniciada; mandato explícito de no iniciar en Fase 4A). | agent-orchestrator | ⏳ PENDIENTE — no iniciada |
| agent-orchestrator (PRICE_MONITORING_AGENT_V1) | **`ASSISTED_BUDGET_AGENT_V1`** — Agente asistido de presupuesto (no iniciado; mandato explícito de no iniciar en Fase 4A). | agent-orchestrator | ⏳ PENDIENTE — no iniciado |
| agent-orchestrator (PROVIDER_PRICE_LIST_MAPPING_UX_FIX_V1) | **`PRICE_LIST_IMPORT_ACCEPTANCE_DECORCERAMICA`** — Repetir prueba de aceptación de lista Decorcerámica tras release del hotfix: cargar `externalSku=6751,KP04NG1620,MAT-PORC-DC-001,...observedPrice=169000` + fila sin match `9999`, confirmar 1 observación pending + 1 sin asociar en reporte. Validar que el nuevo resumen de auto-detección muestre ✓ precio y ✓ identificador sin abrir panel avanzado. | agent-qa / agent-frontend-boq | ⏳ PENDIENTE — post-release hotfix |
| agent-orchestrator (CATALOG_BULK_ONBOARDING_V1) | **`DOCUMENT_LIST_IMPORT_V1`** — Importación desde PDF/DOCX/OCR NO implementada: tablas irregulares, documentos escaneados, riesgo de extracción incorrecta; requiere revisión humana específica. Sin botón placeholder en la UI. | agent-excel-mapper / agent-qa | ⏳ PENDIENTE — deuda registrada en contrato §11 |
| agent-orchestrator (CATALOG_BULK_ONBOARDING_V1) | **`BOQ_TO_CATALOG_ASSISTED_BOOTSTRAP`** — El parser BOQ solo expone actividades de presupuesto. Campos faltantes para bootstrap confiable de catálogo: `resourceType`, desglose de insumos por APU, código de catálogo (vs numeración de presupuesto), brand/ref/SKU, precio de insumo (unitPrice es de actividad compuesta). Relacionada con `COST_TYPE_BREAKDOWN_FOUNDATION`. | agent-cost-domain / agent-excel-mapper | ⏳ PENDIENTE — diferido; documentado en contrato §7 |
| agent-orchestrator (CATALOG_BULK_ONBOARDING_V1) | **`COLUMN_MAPPING_PRESETS`** — Persistir presets de mapeo de columnas exige tabla nueva + RLS + harness; diferido. El mapeo automático por sinónimos cubre el caso común. | agent-db-rls / agent-frontend-boq | ⏳ PENDIENTE — diferido sin bloquear importación |
| agent-orchestrator (CATALOG_BULK_ONBOARDING_V1) | **`LARGE_FILE_DIRECT_UPLOAD`** — Vercel serverless limita el body a ~4.5MB; archivos de importación >4MB requieren upload directo a Supabase Storage (signed upload) + lectura server-side. Local/self-host opera hasta 10MB vía Server Action (bodySizeLimit 12MB). | agent-orchestrator / agent-db-rls | ⏳ PENDIENTE — riesgo residual documentado en DECISIONS |
| agent-orchestrator (Bootstrap CTAs) | **`APU_CREATE_FLOW`** — No existe flujo de creación manual de plantillas APU desde la UI. Los APUs se generan durante la importación de presupuesto. Para permitir que una org vacía pueda crear APUs manualmente se requiere: formulario de APU (code, name, unit) + componentes APU + server action + backend. | agent-frontend-boq / agent-cost-domain | ✅ RESUELTO (2026-06-13, APU_MANUAL_BUILDER_V1) — `/apu/new` con formulario completo materiales + M.O. + toolPct |
| agent-orchestrator (APU_MANUAL_BUILDER_VALIDATION_AND_ARCHIVE_HOTFIX_V1) | **`READ_MODEL_ARCHIVED_AT`** — `apps/web/server/read-model/drizzle-repository.ts` debe exponer `archived_at` de `apu_templates` en `getApuDetail` (populate `ApuDetail.archivedAt`) y `listApus` (populate `ApuLibraryItem.archivedAt`). Actualmente el campo llega `null` en la vista de listado y detalle porque el read-model Drizzle no selecciona esa columna. Workaround: leer directamente vía RLS client en `server/apu-library/service.ts` (ya implementado para el listado; el detalle usa `ApuDetail.archivedAt` del read-model). | agent-db-rls / agent-cost-domain | ⏳ PENDIENTE — limitación conocida, no bloqueante para el release |
| agent-orchestrator (Bootstrap CTAs) | **`QUANTITIES_CREATE_FLOW`** — No existe flujo de creación manual de grupos/líneas de cantidades. Las cantidades se cargan desde scopes del proyecto durante importación. Para org vacía: formulario de grupo + líneas + backend de escritura. | agent-frontend-boq | ⏳ PENDIENTE — diferido post-bootstrap |
| agent-orchestrator (Bootstrap CTAs) | **`PLANNING_CREATE_FLOW`** — No existe flujo de creación manual de tareas de cronograma desde la UI. Para org vacía: formulario de tarea + dependencias + backend de escritura de planning. | agent-planning | ⏳ PENDIENTE — diferido post-bootstrap |
|---|---|---|---|
| agent-pricing (Phase 3B) | **Integración ramas post Operational UX.** Cuando `feature/operational-budget-ux-v1` cierre: (1) merge ordenado de ambas ramas sobre `integration/iconic-ui-price-intelligence-v1`; (2) `supabase db reset --local`; (3) RLS runtime 106/106; (4) smoke MVP e2e; (5) `validate-claude-agents`; (6) revisión visual `/catalog/resources/<id>/price-intelligence` (panel URL visible). Sin migración nueva requerida por Phase 3B. | agent-orchestrator | ✅ RESUELTO (2026-06-09) — `integration/operational-ux-price-validation-v1` HEAD `110a5f6`. Merge secuencial: Operational UX (sin conflictos) → Phase 3B (conflictos docs resueltos). Fix integración: mock `countPendingResourcePriceObservations`. 26 migraciones + 5 seeds. 944/944 PASS, RLS 106/106, smoke 10/10, redirect 15/15. UrlValidationPanel visible en price-intelligence. Sin merge a main; sin deploy. |
| agent-pricing (Phase 3B) | **Adaptadores futuros de proveedores.** Documentados en `PRICE_VALIDATION_AGENT_V1_CONTRACT.md §Adaptadores futuros`. Cuando se requiera: implementar `PriceAdapter` por proveedor (Homecenter, Decorcerámica) como `apps/web/server/pricing/validation/adapters/<nombre>.ts`. Prioridad: post-release Phase 3B. | agent-homecenter / agent-pricing | ⏳ PENDIENTE — diferido post-release |
| agent-excel-mapper | Permiso de **ejecución de Node/Vitest sobre `private/`** para confirmar la regresión empíricamente. | agent-orchestrator | ✅ RESUELTO (2026-05-30, Fase 1) — El orquestador ejecutó `gm:dump`, `gm:build-fixture`, `gm:regression` (22/22 PASS) y `gm:import` (todas PASS) sobre el Excel real. Coordenadas de los 9 valores confirmadas (EXCEL_MAPPING §10). 9/9 valores **empíricos** ±0.01 COP. |
| agent-excel-mapper | (Opcional) Exponer scripts `gm:dump`, `gm:regression`, `gm:import` en `package.json` raíz. | agent-orchestrator | ✅ RESUELTO (2026-05-30) — Añadidos `gm:dump`, `gm:build-fixture`, `gm:regression`, `gm:import` al `package.json` raíz + `tsx` (devDep). |
| agent-excel-mapper | (Aviso) Tras confirmar el fixture sanitizado, evaluar el retiro de `.worktreeinclude`. | agent-orchestrator | ✅ RESUELTO (2026-05-30, Fase 6) — `.worktreeinclude` ELIMINADO en `integration/wave-1`. El fixture sanitizado fila-por-fila es suficiente; Oleada 2 consume el fixture, no el Excel. `private/` sigue ignorado. |
| agent-orchestrator | **Fix de integración en archivo de db-rls** `supabase/seeds/0001_demo_org_and_profiles.sql`: el seed insertaba `profiles` sin crear antes las filas en `auth.users`. En el stack Supabase local **el esquema `auth` SÍ existe**, por lo que la migración `0001` activa el FK `profiles_id_auth_users_fk` y `db reset` falla (`SQLSTATE 23503`). Surgió al ejecutar el RLS runtime real (B-003), antes bloqueado por Docker. | agent-db-rls | ✅ RESUELTO (2026-05-30, Oleada 1.5) — El orquestador añadió un bloque `DO $$ … auth.users … $$` **guardado por la misma condición** que usa la migración (presencia del esquema `auth`), de modo que el seed sigue funcionando en un Postgres puro sin `auth`. Solo se inserta `id` + columnas mínimas (`instance_id/aud/role/email`), sin credenciales ni login real. **db-rls debe revisar/avalar** este patrón de seed de auth para futuras migraciones de perfiles. |
| agent-cost-domain | **Reconciliar `PricingReadPort`/`ApprovedPriceContext` en la integración de Oleada 2A.** Ambos worktrees (cost-domain y pricing) se derivaron de `main`@`974ea99` (antes del commit del contrato `02ca9c3`), así que cada agente implementó el puerto **por su cuenta** desde la spec de la tarea: cost-domain en `apps/web/modules/apu/pricing-port.ts` y pricing en `apps/web/modules/pricing/{types,read-service}.ts`. Hay que unificar a **una sola fuente de tipos** (el contrato congelado `docs/PRICING_READ_CONTRACT.md`) y verificar que las firmas coinciden 1:1. cost-domain solo consume `ApprovedPriceContext.budgetReferencePrice`. | agent-orchestrator | ✅ RESUELTO (2026-05-30, integración 2A) — Creada **FUENTE ÚNICA DE CÓDIGO** `apps/web/lib/contracts/pricing-read.ts` (refleja el contrato congelado: forma async + `PricingReadResult` union + `ApprovedPriceContext` completo). `apps/web/modules/apu/pricing-port.ts` la re-exporta (cost-domain consume); `apps/web/modules/pricing/types.ts` la re-exporta (pricing implementa). **Una sola** `interface PricingReadPort` / `ApprovedPriceContext`. cost-domain pasó su puerto a **async** y convierte `!ok`→`ApprovedPriceNotFoundError`/`AmbiguousApprovedPriceError`. **Ajuste menor del contrato**: `PricingReadQuery.estimateVersionId?` (opcional) para congelar precio por versión (cost-domain); documentado en PRICING_READ_CONTRACT y API_CONTRACTS. typecheck/test/gm revalidados. |
| agent-cost-domain | **Confirmar la base del IVA sin hardcodear.** cost-domain marca la línea de Utilidad con un flag `contributesToUtilityBase` para identificar la base del IVA, en vez de un código/tasa fija. El esquema `indirect_cost_rules` ya tiene `base_type IN ('direct_cost','utility',...)`. Decidir si la base del IVA se deriva de `base_type='utility'` (esquema) o del flag del dominio. | agent-orchestrator / agent-db-rls | ✅ RESUELTO (2026-05-30, integración 2A) — Eliminado el flag `contributesToUtilityBase`. **Regla canónica (solo `base_type`+`sortOrder`)**: una regla `base_type='utility'` se aplica sobre el monto de la **última línea `direct_cost` precedente** (la Utilidad). Reproduce el golden master: IVA = Utilidad × 0.19 (gm:import diff=0). Sin cambios al seed/fixture (U sigue `direct_cost`, semánticamente correcto). Tests añadidos: base directa, base utility, orden de cálculo, error si no hay `direct_cost` previa. db-rls/excel-mapper pueden avalar; si en el futuro hay >1 línea de utilidad, considerar un marcador explícito en el esquema. |
| agent-cost-domain / agent-pricing | **`git commit` denegado en el entorno de ambos subagentes.** Ambos dejaron su trabajo **staged** pero no pudieron commitear. | agent-orchestrator | ✅ RESUELTO (2026-05-30) — El orquestador commiteó cada worktree (cost-domain `3783aca`, pricing `7897926`) y preservó en `backup/wave2-cost-domain` / `backup/wave2-pricing` (pusheadas). Validado: cost-domain 178/178 + gm 22/22; pricing 155/155; typecheck/lint 0 en ambos. |
| agent-homecenter | **Reconciliar el adaptador contra el contrato congelado en la integración 2B.** El worktree del agente se derivó de `main`@`75394f1` (sin `docs/PRICING_ADAPTER_CONTRACT.md`), así que implementó desde la spec de la tarea. Verificar 1:1 contra el contrato: tipos auxiliares `ResourceCatalog`/`ResourceRef` (no estaban en el contrato; sirven al matching, avalar o mover a un lugar canónico); `MinimalApprovalPort`/`ImportApprovalAudit` vs `PricingApprovalPort` real; rutas de import de `scripts/catalog-sync` (`../../apps/web/...index.js`). | agent-orchestrator | ✅ RESUELTO (2026-05-30, integración 2B) — Tipos del adaptador **alineados 1:1** con el contrato (`SupplierAdapter`, `RawSupplierItem`, `SkuMatchCandidate/Proposal`, `ImportPreview`, `ImportResult`); consumen `RecordObservationInput` real de `@/modules/pricing/types`. **`MinimalApprovalPort` confirmado como subconjunto estructural de `PricingApprovalPort`** (NO lógica paralela): añadido `tests/unit/pricing-adapters/port-reconciliation.test.ts` con aserción type-level (`(p: PricingApprovalPort) => MinimalApprovalPort`) + prueba runtime que persiste vía el `PricingApprovalService` real (in-memory) respetando append-only e idempotencia. `ResourceCatalog`/`ResourceRef` se **avalan** como tipos auxiliares de matching dentro de `adapters/`. typecheck/lint/test/build revalidados (309 tests). |
| agent-homecenter | **Entrega con typecheck rojo (43 errores) + `git commit` denegado.** El agente dejó el adaptador con tests en verde (runtime) y lint OK, pero `tsc --noEmit` fallaba (strict-null + `string|undefined` + export `ResourceCatalog` faltante); su corrida se truncó. Además el reintento generó un **segundo worktree** (no continuó el original vía SendMessage). | agent-orchestrator | ✅ RESUELTO (2026-05-30) — El segundo pase dejó adapters+tests en **typecheck 0**; el orquestador **consolidó**: copió `scripts/catalog-sync/` + memoria del worktree original al worktree verde, revalidó (typecheck 0, lint 0, **306 tests**, build OK), commiteó `ccc1f0b` y preservó en `backup/wave2-homecenter` (pusheada). xlsx usado como **devDep** (import dinámico en script): **sin cambios a `package.json`**. |
| agent-dashboard | **Dependencia `recharts`** para gráficos del dashboard (Oleada 3A). Uso: frontend únicamente. Licencia: verificar. Prohibido añadir paquetes innecesarios. | agent-orchestrator | ✅ RESUELTO (2026-05-31) — `recharts` **3.8.1** (**MIT**, permisiva) añadido a `apps/web` vía `corepack pnpm --filter web add recharts`. Sin global. NO se instalaron frappe-gantt/exceljs/@react-pdf/renderer (diferidas a 3B/3C). Registrado en LICENSING y DECISIONS. |
| agent-frontend-boq / agent-dashboard | **Reconciliar en la integración 3A.** (1) Triple `apps/web/lib/contracts/read-model.ts` (db-rls canónico vs variantes de frontend/dashboard) → unificar a la de db-rls. (2) **Accesores TEMP de dev** (`components/budget/dev-read-model.ts`, `modules/dashboard/dev-read-model.ts`) → rewire de las páginas a `@/server/read-model` (`getReadModel()` de db-rls, `READ_MODEL_SOURCE=fixture`). (3) **Divergencias de DTO**: frontend usó `ProjectListItem{code,scopeNames,startDate}` y `EstimateSummary.status:string` vs canónicos `{scopeCount,createdAt,estimateCount}` y `EstimateVersionStatus`. | agent-orchestrator | ✅ RESUELTO (2026-05-31, integración 3A) — **(1)** Conflicto add/add de `read-model.ts` resuelto conservando el canónico de db-rls (`--ours`); duplicados eliminados (1 sola `interface ReadModelPort`). **(2)** Accesores TEMP `dev-read-model.ts` **eliminados**; las 5 páginas y el dashboard consumen `getReadModel()` de `@/server/read-model` con `getDemoViewer()` (nuevo helper `apps/web/server/read-model/viewer.ts`). **(3)** Páginas adaptadas a DTOs canónicos (`scopeCount`/`createdAt`/`estimateCount`; `EstimateSummary.versionId`/`status:EstimateVersionStatus`; `QuantityGroupView{id,name,lines}`). Test redundante del frontend eliminado; test del dashboard repointado al canónico. Revalidado: typecheck 0, lint 0, **356 tests**, build OK, gm 22/22 + 9/9, validador 214/0; **dev smoke 8/8 HTTP 200** con datos reales del golden master. |
| agent-db-rls / agent-frontend-boq / agent-dashboard | **Subagentes 3A interrumpidos por límite de sesión** (no terminaron limpiamente). Cada uno dejó su trabajo casi completo pero `tsc`/build con errores triviales remanentes (db-rls 4 typing; dashboard 2 typing + ruta fixture + require→import + build; frontend 7 typing + ruta fixture). | agent-orchestrator | ✅ RESUELTO (2026-05-31) — El orquestador aplicó **fixes mínimos de integración** por worktree (anotaciones de tipo, casts, import estático del fixture, `env:Partial<ProcessEnv>`, typing de legend Recharts), revalidó cada uno en **verde** (db-rls 329 tests; dashboard 336; frontend 338; los tres typecheck/lint/build OK) y preservó en `backup/wave3-db-read-model` (`7478ceb`), `backup/wave3-dashboard` (`79d9fd3`), `backup/wave3-frontend-boq` (`28d8bfe`). |
| agent-planning | **Dependencia `frappe-gantt`** para la vista Gantt (Oleada 3B). Uso: frontend de planning. Licencia: verificar. Import dinámico (manipula DOM/SVG). | agent-orchestrator | ✅ RESUELTO (2026-05-31) — `frappe-gantt` **1.2.2** (**MIT**, sin peers) añadido a `apps/web` vía `corepack pnpm --filter web add frappe-gantt`. Sin global. NO se instalaron exceljs/@react-pdf/renderer (diferidas a 3C). Registrado en LICENSING y DECISIONS. Debe importarse dinámicamente client-side ('use client'). |
| agent-db-rls / agent-planning | **Oleada 3B INCOMPLETA — subagentes interrumpidos por límite de sesión.** db-rls: migración planning (4 tablas) + policies RLS + schema Drizzle + DTOs/métodos del `ReadModelPort` + `compute-planning` + datos de cronograma en el fixture, PERO **sin** implementar `getSchedule/listProgressEntries/listResourceAssignments` en los repos (typecheck rojo: 4 errores), **sin** seed de planning, **sin** RLS runtime. planning: núcleo de dominio puro (`cpm/graph/date/decimal/types`, typecheck verde) PERO **sin** página `/planning`, **sin** Gantt, **sin** dev-accessor, **sin** tests. | agent-orchestrator | 🟡 PENDIENTE — Preservado WIP en `backup/wave3b-db-planning` (`89bfab7`) y `backup/wave3b-planning-ui` (`6f4dab9`) (pusheados). **Requiere reanudar ambos agentes** (sesión reseteada) para completar antes del ciclo de integración: db-rls implementa los 3 métodos en ambos repos + seed + RLS runtime 21/21+planning; planning añade `/planning` + Gantt + dev-accessor + tests. NO integrado; `main` intacta. |
| agent-db-rls | **Continuación 3B (db-rls) COMPLETADA** (secuencial, desde el WIP `89bfab7`). | agent-orchestrator | ✅ RESUELTO (2026-05-31) — Rama `continuation/wave3b-db-planning`; el agente hizo `git merge --ff-only 89bfab7` (recuperó el WIP sin redo) e implementó: `getSchedule/listProgressEntries/listResourceAssignments` en Fixture+Drizzle (proyección por rol), seed `0003_demo_planning.sql` (cronograma sanitizado) + `config.toml`, +11 pruebas RLS runtime de planning, tests fixture/drizzle. Validado: typecheck 0, lint 0, **378 tests**, build OK, gm 22/22 + 9/9; **RLS runtime 32/32** (21 previos + 11 planning; **24 tablas RLS FORCE**). Preservado en `backup/wave3b-db-planning-complete` (`560b2cc`). **PENDIENTE**: `agent-planning` (página `/planning` + Gantt + dev-accessor + tests) — reanudar secuencialmente desde `backup/wave3b-planning-ui` (`6f4dab9`). NO integrado. |
| agent-planning | **Continuación 3B (planning-ui) COMPLETADA** (secuencial, desde el WIP `6f4dab9`). | agent-orchestrator | ✅ RESUELTO (2026-06-01) — Rama `continuation/wave3b-planning-ui` = WIP planning (`6f4dab9`) **+ read-model db completo (`560b2cc`)** combinados (sin overlap). El agente recuperó la base con `git merge --ff-only 2793bbf` y construyó dominio (gantt-mapping/view-model/index) + componentes (GanttChart/ScheduleTable/PlanningSummary/StatusBadge). El orquestador **completó**: página `/planning` cableada a `getReadModel().getSchedule()` real, tests de dominio (`planning-domain.test.ts`), enlace de nav, reconciliación del `ScheduleSummary` (view-model → contrato canónico `@/lib/contracts/read-model`), CSS de frappe-gantt vendorizado (su `exports` v1.2.2 no expone el subpath), `ganttRef any→unknown`. Validado: typecheck 0, lint 0, **389 tests**, build OK (`/planning`), gm 22/22 + 9/9; **dev smoke `/planning` HTTP 200** con cronograma + Gantt reales. Preservado en `backup/wave3b-planning-ui-complete` (`41abbe2`). **3B db+ui COMPLETO** y combinado en `continuation/wave3b-planning-ui` (`41abbe2`), listo para integración. NO integrado. |
| agent-db-rls / agent-planning | **Integración formal 3B en `integration/wave-3b`.** | agent-orchestrator | ✅ RESUELTO (2026-06-01) — `git merge --no-ff continuation/wave3b-planning-ui` (db+ui combinados) → merge `595e5a5`, **sin conflictos**. Read-model canónico único; `/planning` consume `getReadModel().getSchedule()` real (0 TEMP); CPM solo en `modules/planning` (0 en React); frappe-gantt dinámico + CSS vendorizado; nav `/planning`. Validado: typecheck/lint 0, **389 tests**, build (`/planning`), gm 22/22 + 9/9, validador 214/0, **RLS runtime 32/32** (24 tablas FORCE; 13 migr + 3 seeds), dev smoke **9/9 HTTP 200**. NO merge a `main`. |
| agent-exports | **Dependencia `exceljs`** para generación XLSX server-side (Oleada 3C). Uso: exports server-side. Licencia: verificar. Sin paquetes innecesarios. | agent-orchestrator | ✅ RESUELTO (2026-06-01) — `exceljs` **4.4.0** (**MIT**) añadido a `apps/web` (`corepack pnpm --filter web add exceljs`). Transitiva `jszip` doble `MIT OR GPL` → se elige MIT. Escaneo sin AGPL/enterprise. Solo server-side. Registrado en LICENSING/DECISIONS. |
| agent-exports | **Dependencia `@react-pdf/renderer`** para generación PDF server-side (Oleada 3C). Uso: exports server-side. Licencia: verificar árbol transitivo. | agent-orchestrator | ✅ RESUELTO (2026-06-01) — `@react-pdf/renderer` **4.5.1** (**MIT**) añadido a `apps/web` (`corepack pnpm --filter web add @react-pdf/renderer`). Peer React ^19 OK; árbol transitivo sin AGPL/enterprise; fuentes por defecto. Solo server-side. Registrado en LICENSING/DECISIONS. |
| agent-exports | **Cierre Oleada 3C — reconciliación de divergencia `origin/main` (squash) y merge a `main`.** El PR de GitHub fusionó `integration/wave-3c`@`2621d3b` como **squash** (`origin/main=b09158f`, sin ancestry de `2621d3b`); faltaba integrar el código de exports. | agent-orchestrator | ✅ RESUELTO (2026-06-01) — **Caso B**: rama `integration/wave-3c-main-reconciled` desde `origin/main` + `git merge --no-ff backup/wave3c-exports` → **solo 15 archivos de exports** (sin conflictos, sin duplicar B-005/contrato/deps). Validado: typecheck/lint 0, **410 tests**, build (`/api/exports`), gm 22/22 + 9/9, validador 214/0, **smoke 6/6 HTTP 200** + negativos 400, privacidad por perfil, RLS 32/32 vigente. Merge `--no-ff` a `main` + tag `wave-3c-exports-v1`. `integration/wave-3c`, rama reconciliada y `backup/wave3c-exports` conservadas. **Oleada 3C CERRADA.** |
| agent-db-rls | **Dependencia `@supabase/supabase-js`** (Oleada 4A). Uso: cliente oficial Supabase (auth + DB local). Licencia: verificar. | agent-orchestrator | ✅ RESUELTO (2026-06-01) — `@supabase/supabase-js` **2.106.2** (**MIT**) añadido a `apps/web`. Árbol sin AGPL/enterprise nuevos. Clave publishable/anon (pública); `service_role` NUNCA en frontend. Registrado en LICENSING/DECISIONS. |
| agent-db-rls | **Dependencia `@supabase/ssr`** (Oleada 4A). Uso: sesión SSR por cookies. Licencia: verificar árbol transitivo y peer. | agent-orchestrator | ✅ RESUELTO (2026-06-01) — `@supabase/ssr` **0.10.3** (**MIT**) añadido a `apps/web`. Peer `@supabase/supabase-js ^2.105.3` cumplida (2.106.2). Compatible con Next 16 (App Router/cookies). Sin AGPL/enterprise. Registrado en LICENSING/DECISIONS. |
| agent-db-rls | **Entregable 4A.1 (auth/RLS DB) COMPLETADO** (continuación) — el subagente se interrumpió por límite de sesión tras implementar la migración de helpers de identidad, el seed org B / sin-membresía y los tests auth del harness, **sin commitear** (todo en su worktree). | agent-orchestrator | ✅ RESUELTO (2026-06-01) — El orquestador recuperó el WIP del worktree y validó. Migración `20260601090000_auth_identity_helpers.sql` (helpers `auth.uid()→profiles` con COALESCE a claims demo; `SECURITY DEFINER` para evitar recursión RLS; deny-by-default), seed `0004_auth_org_b_and_no_membership.sql` (org B + roles variados + usuario sin membresía; sanitizado), +15 tests auth en `scripts/rls-runtime/run.ts`. **Reutiliza `profiles` single-org (sin tablas nuevas).** Validado: **RLS runtime 47/47** (32 previos + 15 auth: aislamiento real A/B, sin sesión→deny, sin membresía→deny, cross-org bloqueado, rol admin vs obra, prioridad claim demo); typecheck/lint 0, **410 tests**, build OK, gm 22/22 + 9/9, validador 214/0; sin secretos/.env/privados. Preservado en `backup/wave4a-auth-db-local` (`58de9c3`, pusheado). **NO integrado** a `integration/wave-4a-auth-local` ni `main` (espera aprobación). |
| agent-db-rls | **Integración formal 4A.1 en `integration/wave-4a-auth-local`.** | agent-orchestrator | ✅ RESUELTO (2026-06-01) — `git merge --no-ff backup/wave4a-auth-db-local` → merge `adeafbe`, **sin conflictos** (4 archivos, +401). Auditoría: 14 migraciones + 4 seeds; 7 helpers canónicos; **0 tablas nuevas** (reutiliza `profiles`/`organizations`); SECURITY DEFINER solo donde se requiere. **RLS runtime 47/47** (32 previos + 15 auth); typecheck/lint 0, **410 tests**, build, gm 22/22 + 9/9, validador 214/0; sin secretos. **NO merge a `main`**. Sesión SSR/viewer/proxy/UI → 4A.2. |
| agent-frontend-boq | **UI de acceso (Oleada 4A.2)** — login/logout/forgot/reset/callback consumiendo el runtime SSR ya implementado (`@/lib/supabase/client`, `@/server/auth`). Ownership: `app/(auth)/`, `components/auth/`, `tests/unit/auth-ui/`. | agent-orchestrator | ✅ RESUELTO (2026-06-02) — `agent-frontend-boq` (worktree aislado) entregó la UI; recuperada tras apagado y preservada en `backup/wave4a-auth-ui` (`c9063ad`, incl. fix Suspense `/login` para build Next 16). **Integrada** a `integration/wave-4a-auth-runtime` con `git merge --no-ff` → merge `5c60339`, **sin conflictos** (11 archivos, +1106). Validado: typecheck/lint 0, **452 tests**, build OK (Proxy + rutas auth + `/api/exports`), gm 22/22 + 9/9, validador 214/0. **Smoke local Supabase (Docker, 47/47 RLS)**: login real, guard de rutas, anti-escalamiento de exports, logout, forgot/reset (Mailpit), demo fixture. **NO merge a `main`**; sin remoto/Vercel. |
| agent-orchestrator | **Bootstrap real del esquema remoto (Oleada 4A.3b)** — aplicar las 14 migraciones al proyecto remoto `construction-ops-prod` vía `supabase db push --linked`, sin seeds ni usuarios. | agent-orchestrator | ✅ RESUELTO (2026-06-02) — Merge paridad PG17 a `main` (`139dd52`, tag `wave-4a3-pg17-bootstrap-ready-v1`) + `db push --linked` (sin `--include-seed`) ⇒ **14/14 migraciones Local = Remote**. Esquema remoto presente, **sin datos funcionales**; seeds NO ejecutados, usuarios NO creados; sin SQL/`migration repair`; **Vercel intacto** (`demo`+`fixture`). Tag `wave-4a3-remote-schema-bootstrap-v1`. **Pendiente 4A.3c**: crear organización + usuario admin inicial seguro y conectar login online manteniendo `READ_MODEL_SOURCE=fixture` (requiere aprobación explícita; NO usar `migration repair` automático ante fallos pre-datos). |
| agent-orchestrator | **Aviso de edición en archivo de `agent-frontend-boq`** (fix 4A.3c): `apps/web/app/(auth)/login/page.tsx`. Se delegó el cuerpo de `handleSubmit` a la nueva función pura `runPasswordLogin` (`apps/web/server/auth/login-flow.ts`, propiedad del orquestador) para habilitar tests de cableado. **Sin cambios de diseño, copy ni layout**; solo la lógica del submit. | agent-frontend-boq | 🟢 INFORMATIVO (2026-06-02) — **Integrado a `main`** (merge `ad8f32b`, tag `wave-4a3-online-login-fix-v1`). agent-frontend-boq debe avalar la delegación en la próxima oleada de UI. La superficie visible del formulario es idéntica. |
| agent-orchestrator | **CIERRE Oleada 4A.3 — autenticación online validada en producción.** Smoke real confirmado por la usuaria (`APP_AUTH_MODE=supabase` + `READ_MODEL_SOURCE=fixture`): login admin → `/dashboard` → dashboard fixture sanitizado. Auditoría read-only `migration list --linked` ⇒ 14/14 Local = Remote. | agent-orchestrator | ✅ RESUELTO (2026-06-02) — Tag `wave-4a3-online-auth-validated-v1`. DB remota intacta; sin secret/service-role en frontend; `READ_MODEL_SOURCE=db` NO activado. **Oleada 4A.3 CERRADA.** **Próximo (NO iniciado, requiere aprobación)**: Oleada 4B — creación real de proyectos desde la UI con `READ_MODEL_SOURCE=db` (contrato → repo DB con RLS → server action con `organizationId`/`created_by` derivados server-side → UI formulario → tests → smoke local → smoke remoto controlado). |
| agent-orchestrator | **Oleada 4B.1 — contrato congelado del vertical slice de proyectos** (`docs/PROJECTS_CRUD_CONTRACT.md` v1). Define migración (`created_by`+`description`), `ProjectsWriteRepository` (`createProject`/`getProjectById`), reglas de seguridad y validación. | agent-db-rls / agent-frontend-boq | 🟡 EN CURSO (2026-06-02) — Rama `integration/wave-4b1-real-projects`. **Fase 2**: `agent-db-rls` implementa esquema + repo de escritura + RLS runtime. **Fase 3**: `agent-frontend-boq` implementa server action + UI. Ejecución secuencial; NO merge a main hasta validación local + revisión. → ✅ **COMPLETADO Y VALIDADO (2026-06-02)**: Fase 2 (backup `3a403bd`) + Fase 3 (backup `6f41fd4`) integradas en `2397323`. typecheck/lint 0, 505 tests, build, gm 22/22, validador 214/0/0, **RLS runtime 58/58**, smoke HTTP demo+fixture OK. Fix de integración del orquestador: import de `isCreationModeEnabled` en el test (actions→mode-guard). **NO merge a main**; pendiente smoke interactivo navegador en modo `db` + autorización de merge. → ✅ **CERRADO (2026-06-02)**: mergeado a `main` (`10ac567`, sin conflictos); migración `projects_authorship` aplicada al remoto (**15/15 Local = Remote**, sin seeds, sin proyectos remotos); tags `wave-4b1-projects-code-ready-v1` / `wave-4b1-projects-remote-ready-v1`. Vercel intacto (`supabase`+`fixture`). Pendiente: acción manual de la usuaria (`READ_MODEL_SOURCE=db` + redeploy + smoke remoto). |
| agent-frontend-boq | **Aviso: edición de `apps/web/app/(dashboard)/projects/page.tsx`** (propiedad frontend-boq) en 4B.1 — reescrito a `resolveViewer()` por modo + botón condicionado por `mode-guard`. Es trabajo del propio agent-frontend-boq en su worktree, integrado por el orquestador. | agent-frontend-boq | 🟢 INFORMATIVO (2026-06-02) — Dentro de su ownership; sin acción pendiente. |
| agent-orchestrator | **HOTFIX en archivos de `agent-db-rls`** (migración + RLS): migración nueva `20260602130000_fix_membership_app_grants_and_profiles_self_rls.sql` (GRANT `USAGE`/`EXECUTE` de esquema `app` a `authenticated`; reemplazo de `profiles_select` por `profiles_self_select`). Autorizada por la usuaria como fix de producción (NO se lanzaron agentes). | agent-db-rls | 🟡 A AVALAR — `agent-db-rls` debe revisar/avalar este hotfix de esquema/RLS en su próxima activación: (a) confirmar que el patrón self-RLS es el deseado y planificar el listado de miembros org-wide no recursivo; (b) considerar mover los GRANTs de `app` a una convención estándar (p. ej. junto a la creación del esquema). Validado: RLS runtime 61/61; reversible vía bloque DOWN. → ✅ **APLICADO (2026-06-02)**: mergeado a `main` (`82c2fa7`); migración aplicada al remoto (**16/16 Local = Remote**, sin seeds, sin proyectos remotos); tags `wave-4b1-membership-fix-code-ready-v1` / `wave-4b1-membership-fix-remote-ready-v1`. Pendiente aval formal de `agent-db-rls` + diseño del listado org-wide no recursivo. |
| agent-orchestrator | **Convención de render para `agent-frontend-boq`**: páginas cuyo render dependa de env de **servidor en runtime** (`APP_AUTH_MODE`/`READ_MODEL_SOURCE`) y que NO usen una API dinámica (`cookies()`/`headers()`) deben declarar `export const dynamic = 'force-dynamic'`, o Next las prerenderiza en build con valores obsoletos. Aplicado a `/projects/new` (fix `9e9dd96`, merge `1999ffb`). | agent-frontend-boq | 🟢 INFORMATIVO (2026-06-03) — Tener en cuenta en futuras páginas con guards dependientes de modo. Sin acción pendiente. → **REFORZADO (2026-06-04, merge `0ad7f56`)**: el flag por sí solo no basta si quedó caché estática previa; usar además una **señal dinámica intrínseca** (`await resolveViewer()`/`cookies()`) para que Next/Vercel traten la ruta como dinámica intrínseca y no la sirvan desde caché estática/edge. |
| agent-orchestrator | **DEUDA — rutas hermanas estáticas deben migrarse a viewer real request-time.** Tras el fix de `/dashboard` (merge `b942f3f`), las páginas `/apu`, `/catalog`, `/quantities` y `/planning` siguen siendo **estáticas (`○`)** y usan `getDemoViewer()` (org demo fija), por lo que se prerenderizan en build resolviendo datos de la organización demo, no la del usuario autenticado. No rompen el build (manejan lista vacía / error capturado) ni bloquean la creación del primer proyecto, pero **antes de uso productivo multitenant** deben migrarse al patrón request-time (`export const dynamic = 'force-dynamic'` + `await resolveViewer()`) ya aplicado a `/projects`, `/projects/new` y `/dashboard`. | agent-frontend-boq / agent-planning | ✅ RESUELTO (2026-06-04, merge `c6d0ad1`) — Estabilización de producción: `/apu`,`/catalog`,`/quantities`,`/planning` (y `/estimates`) migradas a `force-dynamic` + `resolveViewer()` (viewer real en db, demo en fixture); `/planning` sin UUID demo; layout `(dashboard)` `force-dynamic` (cubre el segmento); footer mode-aware (`readModelModeLabel`). Validado: 546 tests, build `db` local vacío/sembrado PASS, Production deploy READY + smoke (rutas protegidas 307→/login, sin fixture público). |
| agent-orchestrator | **Estabilización 4B.1 — CTAs de creación rotos** (`<Link><Button></Button></Link>` ⇒ `<a><button></button></a>`, el botón anidado no navega). Corregido a `<Button asChild><Link href="/projects/new">…` en `/projects` (2 CTAs) y `/dashboard`. | agent-frontend-boq / agent-dashboard | 🟢 INFORMATIVO (2026-06-04) — Dentro de integración del orquestador; convención: para que un botón navegue usar `Button asChild` envolviendo `Link`, nunca `Link` envolviendo `Button`. Sin acción pendiente. |
| agent-orchestrator | **Deuda menor — import muerto `getDemoViewer` en `app/api/exports/route.ts`** (propiedad agent-exports, Oleada 3C). No afecta seguridad: el guard de auth usa `resolveAuthenticatedViewer` en modo supabase. | agent-exports | 🟡 PENDIENTE (2026-06-04) — Limpieza trivial a abordar por agent-exports en su próxima activación (remover el import no usado y confirmar que la generación de exports en `db` use el viewer/proyecto reales del solicitante). Fuera del alcance de la estabilización del vertical slice. |
| agent-orchestrator | **APROBACIÓN REQUERIDA — migración de authorship de `project_scopes` (4B.2).** Diagnóstico Fase 1: el esquema de `project_scopes` carece de `description` (el formulario de alcance pide nombre + descripción) y de `created_by` (paridad de autoría con `projects`). RLS ya es suficiente (FORCE + `project_scopes_all` por org del proyecto padre). Migración propuesta (aditiva, reversible, espejo de `20260602120000`): `ALTER TABLE project_scopes ADD COLUMN description text, ADD COLUMN created_by uuid REFERENCES profiles(id) ON DELETE SET NULL;` + `CREATE INDEX project_scopes_created_by_idx`. Remoto 16/16 ⇒ sería la 17.ª (requiere `db push` controlado tras `--dry-run`). | usuaria / agent-db-rls | ✅ RESUELTO (2026-06-04) — **Aprobado por la usuaria.** Migración `20260604120000_project_scopes_authorship` creada y validada local (`db reset`, RLS runtime 67/67); `db push --dry-run` = 1 migración esperada (sin seeds); `db push --linked` ⇒ **17/17 Local = Remote**. Implementados contrato (`docs/SCOPES_CRUD_CONTRACT.md`), repo `apps/web/server/scopes/`, UI y tests (588 total). Decisiones confirmadas: `scope_type` select (default `floor`), `created_by` server-side. |
| agent-orchestrator | **APROBACIÓN REQUERIDA — migración de estimates (4B.3).** Diagnóstico Fase 1: `estimates` carece de `description` (el formulario/smoke usan descripción) y de `created_by` (paridad; `estimate_versions.created_by` ya existe). RLS suficiente (sin cambios). "Versión activa" = mayor `version_number` (sin columna). La creación atómica estimate+V01 sin service-role requiere una función `SECURITY INVOKER` (un trigger colisionaría con los seeds que ya insertan versiones). Migración propuesta (aditiva, reversible): (1) `ALTER TABLE estimates ADD COLUMN description text, ADD COLUMN created_by uuid REFERENCES profiles(id) ON DELETE SET NULL;` + índice `estimates_created_by_idx`; (2) `public.create_estimate_with_initial_version(...)` `SECURITY INVOKER` (estimate `status='active'` + V01 `draft`) + `GRANT EXECUTE TO authenticated`. Remoto 17/17 ⇒ sería la 18.ª (requiere `db push` tras `--dry-run`). | usuaria / agent-db-rls | ✅ RESUELTO (2026-06-04) — **Aprobado con ajuste de seguridad**: la RPC NO acepta `p_created_by`; deriva el autor de `app._auth_uid()` (helper canónico mig. 20260601090000). Migración `20260604130000` creada y validada local (db reset, RLS runtime **77/77**, ACL sin `anon`); `db push --dry-run` = 1 migración esperada; `db push --linked` ⇒ **18/18 Local = Remote**. Implementados contrato (`docs/ESTIMATES_CRUD_CONTRACT.md`), repo `apps/web/server/estimates/`, UI y tests (623 total). Estimate inicial `active`, V01 `draft`. |
| agent-orchestrator | **APROBACIÓN REQUERIDA — migración de importación BOQ (4C.1).** Diagnóstico Fase 1: el importador existente (`scripts/excel-import/`) es build-time (valida fixture/regresión, NO escribe DB); `xlsx` (SheetJS) disponible server-side; tablas `chapters`/`boq_items` + su RLS ya existen y aceptan inserts en V01 (draft, no bloqueada). PERO la importación atómica (capítulos+ítems juntos, rollback total) y el bloqueo de doble-submit requieren una RPC `SECURITY INVOKER` (no posible con supabase-js multi-tabla sin service-role). Migración propuesta (aditiva, reversible, sin tablas nuevas): `public.import_boq_into_version(p_version_id uuid, p_chapters jsonb, p_items jsonb)` `SECURITY INVOKER` — deny sin sesión; versión debe estar VACÍA (anti doble-import); inserta capítulos (code→id) + ítems con subtotal recomputado server-side; devuelve conteos+total; hardening `REVOKE FROM PUBLIC+anon`/`GRANT TO authenticated`. Remoto 18/18 ⇒ sería la 19.ª (requiere `db push` tras `--dry-run`). | usuaria / agent-db-rls | ✅ RESUELTO (2026-06-05) — **Aprobado por la usuaria** con formato Excel v1 congelado (hoja COTIZACION 1 PISO, columnas A-F) y flujo preview→confirm con digest SHA-256. Migración `20260604140000_boq_import_atomic` creada y validada local (db reset, RLS runtime **87/87**, ACL sin anon); `db push --dry-run` = 1 migración; `db push --linked` ⇒ **19/19 Local = Remote**. Implementados contrato (`docs/EXCEL_IMPORT_CONTRACT.md`), parser, repo preview/confirm/status, UI y tests (651 total). |
| agent-orchestrator | **APROBACIÓN REQUERIDA — migración de trazabilidad de origen (4C.3).** Diagnóstico Fase 0: para normalizar códigos duplicados/inconsistentes conservando trazabilidad reversible, `chapters` y `boq_items` necesitan `source_code text` + `source_row integer` (hoy no existen; `boq_items.notes` no sirve para capítulos ni es estructurado). La RPC `import_boq_into_version` se extiende (MISMA firma `(uuid,jsonb,jsonb)`; el JSONB lleva `sourceCode`/`sourceRow`) para persistirlos. Migración propuesta `20260605120000_boq_source_traceability` (aditiva, reversible): `ALTER TABLE chapters/boq_items ADD COLUMN source_code text, source_row integer` + `CREATE OR REPLACE FUNCTION import_boq_into_version`. `canonicalCode` se guarda en `code`. Remoto 19/19 ⇒ 20.ª (requiere `db push` tras `--dry-run`). | usuaria / agent-db-rls | ✅ RESUELTO (2026-06-05) — **Aprobado** con hardening de mapping por `rowType+sourceRow`. Migración `20260605120000_boq_source_traceability` creada y validada local (db reset, RLS runtime **89/89**, checks `source_row>0`); `db push --dry-run` = 1 migración; `db push --linked` ⇒ **20/20 Local = Remote**. Parser propone canónicos (genérico 7→11…10→14; prefijos de ítems), conserva sourceCode/sourceRow; UI "Revisar numeración" editable; reconstrucción/validación server-side (overrides). 650 tests. |
| agent-orchestrator | **Deuda futura — `AIU_PRESETS_BY_ORGANIZATION`** (4D.2). Permitir plantillas AIU sugeridas por empresa/tipo de obra, editables; NUNCA defaults globales automáticos (los porcentajes siguen siendo por versión y confirmados por la usuaria). | agent-pricing / agent-cost-domain | 🟡 PENDIENTE (2026-06-05) — Registrada; fuera de alcance 4D.2. Abordar en fase posterior. |
| agent-orchestrator | **Deuda futura — `AIU_IMPORT_PREFILL_FROM_EXCEL`** (4D.2/4C). El Preview de Excel (sección AIU detectada y excluida en 4C.2) podrá SUGERIR los porcentajes AIU; la usuaria los confirma antes de guardar; nunca se convierten en defaults globales ni se escriben automáticamente. | agent-excel-mapper / agent-frontend-boq | 🟡 PENDIENTE (2026-06-05) — Registrada; fuera de alcance 4D.2. |
| agent-orchestrator | **Touch cross-ownership avalado — `EstimatesWriteRepository` extendido con `getEstimateExportPayload` (4E.1).** El export del presupuesto real requiere organización + ciudad (lecturas RLS-bound) además de capítulos/ítems/AIU/resumen, por lo que se añadió el método a la interfaz (`server/estimates/types.ts`, propiedad agent-db-rls) y a ambas implementaciones (db/fixture). La GENERACIÓN (exceljs/@react-pdf) vive en el servicio nuevo `server/estimates/export/`, fuera del repo. | agent-db-rls | 🟢 INFORMATIVO (2026-06-05) — Aditivo, sin migración, sin tocar RLS; el método compone lecturas existentes + 1 join extendido (organizations.name, projects.location). db-rls puede avalar el método en una revisión posterior. |
| agent-orchestrator | **Deuda futura — `EXPORT_TRACEABILITY_BY_ROLE`** (4E.1). La hoja `TRAZABILIDAD` del Excel (código original/fila/normalización) debe poder ocultarse o excluirse según el rol del solicitante (p. ej. cliente). Hoy se incluye siempre en el Excel y NUNCA en el PDF. | agent-exports / agent-qa | 🟡 PENDIENTE (2026-06-05) — Registrada; fuera de alcance 4E.1. |
| agent-orchestrator | **Deuda futura — `EXPORT_PROFILES_FOR_ESTIMATE`** (4E.1). Aplicar perfiles de privacidad (cliente vs interno) al nuevo camino de export del presupuesto real, reutilizando la noción de perfiles de la Oleada 3 (`modules/exports`) sobre los datos del `EstimateRepository`. | agent-exports | 🟡 PENDIENTE (2026-06-05) — Registrada; fuera de alcance 4E.1. |
| agent-orchestrator | **Deuda funcional — `ICONIC_LOGO_ASSET`** (4E.1B). Falta el PNG oficial de ICONIC. El sistema está listo: convertir el PNG a base64 y pegarlo en `apps/web/server/estimates/export/logo-asset.ts` (`ICONIC_LOGO_BASE64`); ruta/forma documentadas en `apps/web/public/branding/README.md`. Sin asset, los exports usan el monograma `IC` (no se rompen). | usuaria / agent-exports | ✅ RESUELTO (2026-06-06, 4E.1C) — La usuaria entregó `grupo-iconic-logo-full.png` (846×846) y `grupo-iconic-logo-symbol.png` (1080×1080) + guía visual. Embebidos por `scripts/branding/embed-iconic-assets.mjs` → `logo-asset.ts` (data URI, sin fs runtime). Paleta oficial aplicada; dorado eliminado. PDF/Excel usan los logos oficiales. |
| agent-orchestrator | **APROBACIÓN REQUERIDA (BLOQUEANTE) — migración mínima del invariant DB-level de subtotal (4E.2A).** Diagnóstico FASE 1: la RLS de `chapters`/`boq_items` YA permite insert/update por organización en versiones editables y bloquea emitidas/cross-org ⇒ no requiere cambios de RLS. PERO `boq_items.subtotal numeric(20,10)` solo tiene CHECK `>= 0`; el recálculo `round(qty×price,10)` solo existe en la RPC `import_boq_into_version` (carga inicial). Con el editor manual habilitando escrituras directas, un `authenticated` podría persistir un subtotal arbitrario (positivo) vía PostgREST. **Migración propuesta (aditiva, reversible, sin tablas/RLS)**: `public.set_boq_item_subtotal()` (`round(NEW.quantity_snapshot × NEW.unit_price_snapshot, 10)`) + trigger `boq_items_recompute_subtotal BEFORE INSERT OR UPDATE ON public.boq_items` (sobre TODO insert/update, no solo `OF qty,price`, para cerrar el `PATCH subtotal`-solo). Compatible con la RPC de import (mismo `round(,10)`) y con los exports. Remoto 20/20 ⇒ sería la 21.ª (requiere `db push` tras `--dry-run` cuando se apruebe). | usuaria / agent-db-rls | ✅ RESUELTO (2026-06-07) — **Aprobada por la usuaria** con el ajuste `BEFORE INSERT OR UPDATE`. Migración `20260606120000_boq_items_subtotal_invariant.sql` creada y validada local (db reset PG17, **RLS runtime 106/106** con 17 checks 4E.2A: INSERT/UPDATE recalcula, PATCH-subtotal ignorado, import compatible, origen preservado, mover, negativo/emitida bloqueados); `db push --dry-run` = 1 migración esperada (0 seeds); `db push --linked` ⇒ **21/21 Local = Remote**. db-rls puede avalar el trigger en su próxima activación. |
| agent-orchestrator | **Deuda futura — `BOQ_SAFE_DELETE_OR_ARCHIVE`** (4E.2A). Eliminación segura / soft-delete / archive de capítulos e ítems BOQ, con preservación de trazabilidad y sin tocar versiones emitidas. Explícitamente FUERA del alcance 4E.2A (edición NO destructiva). | agent-cost-domain / agent-db-rls | ✅ RESUELTO (2026-06-09, 4E.2B) — Implementado en `feature/wave-4e2b-boq-safe-archive` (contrato `docs/BOQ_DELETE_ARCHIVE_CONTRACT.md`; migración local `20260609120000`; archive/restore + exclusión en cálculos/exports/read-model; 28 casos cubiertos). Pendiente: merge a integration/main + `db push` + deploy (a tu orden). |
| agent-orchestrator | **Deuda futura — `BOQ_REORDER`** (4E.2A). Reordenamiento de capítulos/ítems (drag-and-drop o botones subir/bajar) recalculando `sort_order`. En 4E.2A `sort_order` solo se asigna `max+1` en creación y se conserva en edición. | agent-frontend-boq | 🟡 PENDIENTE (2026-06-07) — Registrada; fuera de alcance 4E.2A. |
| agent-orchestrator | **Deuda futura — `BOQ_AUDIT_TRAIL`** (4E.2A). Historial de cambios (quién/cuándo/qué) de capítulos e ítems editados manualmente. No incluido en 4E.2A. | agent-db-rls / agent-qa | 🟡 PENDIENTE (2026-06-07) — Registrada; fuera de alcance 4E.2A. |
| agent-orchestrator | **Deuda futura — `ESTIMATE_VERSIONING`** (4E.2A). Nuevas versiones de presupuesto, clonación y comparación de versiones. No incluido en 4E.2A (edición sobre V01 editable). | agent-cost-domain / agent-db-rls | 🟡 PENDIENTE (2026-06-07) — Registrada; fuera de alcance 4E.2A. |
| agent-orchestrator | **Cierre 4E.2A automated-ready (2026-06-07)**: la usuaria omitió el smoke de escritura productiva; 4E.2A verificada por smoke automatizado no destructivo (repo real + RLS local) + producción read-only. Tag `wave-4e2a-manual-boq-editing-automated-ready-v1`. Smoke de escritura real en producción = OPCIONAL futuro. | usuaria | ✅ RESUELTO (2026-06-07) — Sin cambios productivos; ENTRE PATIOS intacto. |
| agent-orchestrator | **Reanudar P1-A SOLO en worktree aislado** (`fix/security-p1a-read-model-rls-export-legacy`, `a78839c` + 2 stashes). Ruta sugerida `D:\ICONIC\SOFTWARE PRESUPUESTOS\construction-ops-security-p1a`. NO recuperar/aplicar/borrar stashes desde el repo principal. | agent-orchestrator / agent-qa | 🟡 PENDIENTE — Acción siguiente recomendada; P1-A permanece pausada e intacta. |
| agent-orchestrator | **4E.2B integrada + 4E.3A implementada (2026-06-09)**: `integration/p1a-functional-resume` = `9f28c26` (4E.2B mergeada); `feature/wave-4e3a-estimate-issue-clone` (emisión/clonación de versiones). Migraciones locales `20260606120000`/`20260609120000`/`20260609130000` **pendientes de `db push` remoto**; merge a main + deploy pendientes (a tu orden). | usuaria / agent-db-rls | 🟡 PENDIENTE — Sin tocar main/producción/remoto en esta sesión. |
| agent-orchestrator | **4E.3A integrada (2026-06-09)**: `integration/p1a-functional-resume` = `cd18f2d`. | usuaria | ✅ RESUELTO (2026-06-09) — validado; main intacta. |
| agent-orchestrator | **DECISIÓN DE PRODUCTO REQUERIDA — 4E.3B blocker de unicidad de ítems.** La comparación de versiones necesita una clave de ítem determinística única; `boq_items` no tiene unicidad por `code` (solo `chapters_version_code_uq` para capítulos). `createBoqItem`/import permiten códigos de ítem repetidos. Opciones: (a) migración `CREATE UNIQUE INDEX boq_items_version_chapter_code_uq (estimate_version_id, chapter_id, code)` — impacto en createBoqItem + import (23505 si hay dup); (b) clave determinística `chapterCode+itemCode+ocurrencia` por sort_order, sin migración. 4E.3B detenida en FASE 5; contrato congelado (`docs/ESTIMATE_VERSION_COMPARE_CONTRACT.md`). | usuaria / agent-db-rls | ✅ RESUELTO (2026-06-09) — **Opción B aprobada**: matching por `chapterCode+itemCode+occurrenceIndex` (orden `sort_order ASC, id ASC`), `duplicateCodeWarning` + aviso UI, **sin migración**. Deuda futura `lineage_id` antes de reorder avanzado. |
| agent-orchestrator | **Pre-release: reconciliar migraciones pendientes** con `supabase db push --dry-run --linked` (read-only). NO asumir que `20260606120000` sigue pendiente; aplicar solo las faltantes confirmadas (`20260606120000`/`20260609120000`/`20260609130000` son candidatas locales). | usuaria / agent-db-rls | 🟡 PENDIENTE — Verificar antes de cualquier `db push`. |
| agent-orchestrator | **MVP interno LOCAL-READY (2026-06-09)**: 4E.3B integrada en `integration/p1a-functional-resume` (`9695322`); smoke end-to-end 10/10; checkpoint/tag `mvp-internal-local-ready-v1`. Doc `docs/MVP_INTERNAL_LOCAL_READY.md`. | usuaria | ✅ RESUELTO — Rama candidata para pre-release; main/producción intactas. Siguiente: pre-release controlado, no nueva feature. |
| agent-orchestrator | **OPERATIONAL BUDGET UX V1 (2026-06-09) — extensión aditiva de contratos**: `EstimatesWriteRepository.countIssuedEstimateVersions(viewer)` (db: count head RLS-bound sobre `estimate_versions.status='issued'`; fixture: derivado) y `PriceObservationRepository.countPendingResourcePriceObservations(viewer)` (db: count head con filtro explícito `organization_id` + `status='pending'`; fixture: derivado). Solo lectura, para KPIs del dashboard operativo. Reflejado en `docs/OPERATIONAL_BUDGET_UX_V1_CONTRACT.md` §6. | agent-db-rls / agent-pricing | ✅ RESUELTO (2026-06-09) — implementado en ambas fuentes (db+fixture) con tests; sin cambios de esquema ni RLS. |
| agent-orchestrator | **DEUDA `COST_TYPE_BREAKDOWN_FOUNDATION` (2026-06-09)**: el desglose por tipo de costo (materiales/mano de obra/equipos/subcontratos/otros) NO es confiable hoy — `boq_items` no clasifica tipo de costo (`apu_template_id` nullable; import/manual sin clasificación). Requiere fundación: clasificación en catálogo/APU + snapshot de tipo en BOQ + reglas de import. El workspace entrega desglose por capítulo (confiable). | agent-cost-domain / agent-db-rls | 🟡 PENDIENTE — slice futuro; no bloquea la oleada. |
| agent-orchestrator | **DEUDA `COMMERCIAL_SIMULATION_PERSISTENCE` (2026-06-09)**: persistir escenarios del Simulador comercial (tabla tenant-scoped + RLS + FORCE count + trazabilidad server-side `created_by`). En esta oleada el simulador es read-only por decisión registrada en DECISIONS.md. | agent-db-rls / agent-cost-domain | 🟡 PENDIENTE — siguiente slice si la usuaria lo prioriza. |
| agent-orchestrator | **REVIEW_CENTER_V1 (2026-06-11) — touch cross-ownership avalable**: `DbCatalogImportRepository` (catálogo) extendido con `createObservationBatch` + `ObservationInsert.importBatchId`; `confirmCatalogImport`/`confirmProviderPriceList` crean un batch por confirmación (digest SHA-256 persistido) y etiquetan observaciones. Migraciones `20260614090000`/`090100` (batches + bulk_actions + `import_batch_id`, FORCE 30) SOLO locales, **pendientes de `db push` remoto en el release**. Test `import-service.test.ts` ajustado solo en allowlist de tablas; `workspace-route-config.test.ts` ajustado al KPI enlazado. | agent-db-rls / agent-pricing | 🟡 A AVALAR — revisar batch/trigger same-org y políticas en la próxima activación de agent-db-rls; el release debe incluir `db push --dry-run` → exactamente 2 migraciones nuevas esperadas. |
| agent-orchestrator | **SIGUIENTE SLICE RECOMENDADO (2026-06-11)**: `ENTRE_PATIOS_APU_IMPORT_V1` + `BOQ_APU_LINKING_V1` (importar APU reales del golden master sobre la fundación 4B.1 y vincular ítems BOQ a templates APU). NO iniciado en esta sesión por mandato explícito. | usuaria / agent-cost-domain / agent-excel-mapper | 🟡 PENDIENTE — requiere mandato de la usuaria. |
| agent-orchestrator | **APU_COMPONENT_RESOURCE_RECONCILIATION_V1 + APU_LIBRARY_OPERATIONAL_UX_V1 (2026-06-13)**: migración aditiva `20260617090000` (cols en `apu_components`, trigger recompute, tabla auditoría `apu_component_resource_actions` FORCE 34→35, 3 RPCs SECURITY INVOKER). **Falta sección [23] en `scripts/rls-runtime/run.ts`** (cross-org/idempotencia/FORCE runtime) — NO añadida porque Docker estaba apagado y no se pudo validar el harness; cobertura SQL equivalente corre estática (`rls-apu-reconciliation-static`). Release requiere `db push --dry-run` ⇒ **exactamente 1 migración nueva**. | agent-db-rls / agent-qa | 🟡 PENDIENTE — añadir sección [23] y ejecutar harness con DB local antes del release. |
| agent-orchestrator | **DEUDA REGISTRADA (2026-06-13)**: `APU_EXPORTS_V1` + `BUDGET_EXPORT_WITH_APU_ANNEX_V1` (desde un presupuesto: descargar PDF/Excel del presupuesto + APU vinculados PDF/Excel + paquete completo con anexos). `APU_UI_ADVANCED_EDITING_V1`, `APU_REUSABLE_CREW_TEMPLATES_V1`, `APU_COMPONENT_DESCRIPTION_BACKFILL`, `APU_COMPONENT_REJECT_STATUS`. NO iniciar sin mandato. | usuaria / agent-exports / agent-cost-domain | 🟡 PENDIENTE — slices futuros. |
| agent-orchestrator | **CIERRE RUNTIME APU reconciliation (2026-06-13)**: con Docker arriba se ejecutó `db reset --local` (migración `20260617090000` limpia), se añadió la **sección [23]** al harness `scripts/rls-runtime/run.ts` (20 checks; pre-flight FORCE 34→35) ⇒ **214/0**, y read-model isolation 12/0, gm:import PASS, smoke gated 42/42. Ya NO queda harness pendiente. Release: `db push --dry-run` ⇒ **exactamente 1 migración** (`20260617090000`). | agent-db-rls / agent-qa | ✅ RESUELTO (2026-06-13) — gates locales completos; pendiente solo revisión visual + release controlado. |
